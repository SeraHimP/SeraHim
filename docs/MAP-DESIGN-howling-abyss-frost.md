# 嚎哭深渊·冰封风格重做 —— 设计文档

> **状态：设计稿，等待用户确认，未写一行实现代码。**
> 与 `docs/VISUAL-UPGRADE-PLAN.md` 顶部的硬性流程要求一致：这份文档确认之前
> 不会动代码；确认过程中如果发现任何拿不准的细节，会在这里标出来问，不会
> 自己猜一个就往下写。
> 本文档实时更新——每次改动在下面"版本记录"加一行，不新建文件、不覆盖历史。

## 版本记录

| 版本 | 日期 | 改动 |
|---|---|---|
| v0.1 | 2026-09-04 | 首版草稿，基于用户本轮需求 + 对现有 `howling_abyss.js`/`WallLayer.js`/`TerrainLayer.js`/`VegetationLayer.js` 的代码勘查写成 |
| v0.2 | 2026-09-04 | 用户确认第 6 节待确认清单并追加细节，开始实现：①孤灵小岛只放 1~2 个（不是多个）；②浮冰/冰川可以多放，且允许大片连成一块，整体定位是"锦上添花的装饰"，不用太密；③墙不是连续一整条，是一段段的，中间用插进地面的石柱子连接；④用 AskUserQuestion 确认过：石柱子摆在现有 13 处缺口位置旁边（复用 `HA_TERRAIN.obstacles` 的弧长位置，不新引入一套独立间距），相邻两根柱子间接一段墙；⑤缺口处的"破碎效果"取更明显那档——墙/桥面在缺口处直接缺一块（豁口状），周围散落更大块的瓦砾。技术方案相应调整：放弃"在通用 `WallLayer` 里新增贴边窄墙带分支"这个思路，改成直接基于本图已有的桥弧长参数化坐标（`P(d, off)`）写一个嚎哭深渊专用的桥体装饰生成器——因为墙/柱子/豁口的位置本来就要跟 13 个既有缺口弧长对齐，用通用的 isWalkable 边界扫描反而要多一层坐标换算，直接复用地图自己的参数化坐标更简单可靠，通用的贴边墙机制留到以后真的有第二张图需要时再抽象。|
| v0.3 | 2026-09-04 | 第一版 demo 做完后用户看实机截图给了两轮返工反馈，均已落地：**①墙体重做**——第一版是"两根柱子中间摆一个两端内缩的长方体"，用户原话"我看起来就是个雷霆方块吧"，问题有三：端头内缩的缝看着像没接上（不是缺损）、单个光滑长方体没有砌墙的纹理感、太厚太满。改成分块砌墙：每段墙按 `WALL_BLOCK_LEN` 切成若干石块（主体一层 + 顶部错缝排列的窄压顶石），首尾两块必须画（贴住柱子中心，视觉上不留豁口），中间的块以 `WALL_GAP_CHANCE`（0.4）概率随机挑一块跳过、改放散落瓦砾（"某些地方缺损"而不是每段都缺）；石块按坐标哈希取 `WALL_STONE_SHADES`（3 种）深浅色阶——项目没有贴图资源，垒砌纹理感全靠"分块+错缝+深浅色"堆出来；墙厚度 `WALL_THICK` 从 22 降到 13（用户："厚度减少一些"）。**②冰面重做（两小轮）**——第一版是"网格里撒一堆小浮冰、命中的格子再在附近聚一簇"，用户反馈"那些小点点，太密集了"；改成默认每个格子摆一块明显大于格距的冰块（让相邻块自然重叠/咬合，边界当裂纹），覆盖率接近铺满，只有格子落在桥附近（`WATER_NEAR_BRIDGE_R`=260 内）才按小概率（`WATER_EXPOSE_CHANCE`=0.32）整块换成"裸露水面+几块碎冰"，对应"只有靠近桥的附近才偶尔出现碎冰伴随着可见的水流（但是大部分都还是冰）"。第一次改完自己截图复核时发现二十面体（IcosahedronGeometry）就算把高度压扁，20 个朝向各异的三角面在斜射光下还是显出尖锐山头的轮廓，看着像碎石山地不像平铺的冰——这是自己发现的执行质量问题，不是用户提的，判断属于"把已确认的设计意图做到位"的技术执行范畴，没有另外发起确认就直接改了：把冰面/水面/碎冰的几何从二十面体换成矮圆柱（`CylinderGeometry`，真正有一个水平顶面），换完之后俯视效果才读成一整块带裂纹的平冰，不再像小山。|
| v0.4 | 2026-09-04 | 用户又发来一版实机反馈（截图+文字），这次是真 bug，不是审美偏好：**①冰块摆放 bug**——`CylinderGeometry(1,...)` 半径是 1，`mesh.scale.set(s,s,s)` 里的 `s` 当时是按"接近格距 STEP 的值"算的（0.66~1.16 倍 STEP），但 `s` 在 three.js 里是半径不是直径，实际半径因此暴涨到格距的 1.3~2.3 倍——相邻冰块大面积互相重叠，用户原话"浮冰几乎覆盖了整个水面堆得乱七八糟"；而且"能不能摆"的判定只查了块中心点在不在桥上，块本身那么大，中心不在桥上边缘早就伸进桥里了，这就是"穿模到桥里面"的真正原因。改法：把 `s` 直接当半径算（目标半径≈格距一半的 0.72~0.98 倍，只留轻微咬合），"能不能摆"改成中心+四个方向边缘采样点全部不在桥上才摆（`clearOfBridge`），并把非靠桥区域的摆放概率从"必摆"降到 `ICE_FILL_CHANCE`=0.58，露出可见的水面而不是铺满。**②颜色区分度**——用户："路的颜色你最起码要和冰的颜色做区分""水面甚至还是灰色，水应该是稍深的蓝色"。查下来颜色数据本身没错（直接读取 three.js 材质对象确认过 hex 值完全等于代码里写的），但冰色（`#dfeaf0`/`#c9dee8`）跟桥面雪色（frost 调色板 `corridorColor` `#eef4f8`）几乎是同一个色，本来就分不清；水色 `#1c3946` 太暗，这次冰色改成明显更蓝更饱和的 `#7fb2cc`/`#5f9ab8`，水色改成更亮更蓝的 `#1f6f95`，跟雪白桥面拉开三级色差。**（未解决、已知悬而未决）** 改完之后实机截图冰块和水色仍然发灰发绿，不是预期的蓝——用直接读取材质色值的方式确认过，代码里写的确实是蓝色，问题出在场景光照/色调映射（tonemap）把偏冷的颜色统一往暖色/低饱和方向压，这不是这张图独有的 bug，是渲染层此前就记录过的开放问题（第 1 节描边 bug 相关章节之外的另一处），改动会影响到所有地图的光照/色调映射代码，按 `docs/VISUAL-UPGRADE-PLAN.md` 顶部"改视觉代码前必须先问"的硬规矩，这次没有擅自动共享渲染代码，留给用户决定要不要连带处理。**③按用户指示暂停**——用户："算了不要再做浮冰/冰川/岛等了，以后再说，先把主体做完"，水域装饰（冰/水/孤灵小岛）在①②的 bug 修完后按现状搁置，不再继续投入，先完成桥体主体。**④墙体缺损位置对应桥的真实缺口**——用户（四个感叹号强调）："桥中间那里的缺口，对应的那块的墙也要损毁"。v0.3 的缺损是"整段墙里随机挑一块中间的"，跟桥本来就有的 13×2 处缺口（`HA_TERRAIN.obstacles`）位置没有任何关系，两处"缺损"各画各的。查设计文档 v0.2 才发现这本来就是原定方案（"豁口本身落在柱子的位置上"），v0.3 重做分块墙时为了解决"墙接不上柱子"的问题把首尾块改成"必须画实心"，结果连带把缺损位置的语义也弄丢了。这次改成：贴着柱子的那一块墙才是候选缺损位置（不是段中间），每段墙两端各自独立按 `WALL_GAP_CHANCE`（0.4→0.55，加大概率让对应关系更明显）判定是否损毁，段太短（少于 3 块）时只留一端可损毁，避免整段墙被拆空；"贴住柱子不留豁口"和"缺损在哪"两件事分开处理——块的位置依旧紧贴到底，缺损时用瓦砾堆替换石块本体，不会显得几何断开。|
| v0.5 | 2026-09-05 | 用户实机试玩后一次性给了 4 条反馈（Q1~Q4），已落地 3 条，第 4 条（Q3）涉及仿真层，还在等用户拍板方案：**Q1-冰块真的穿模到桥上（真 bug）**——`clearOfBridge` 上一轮改成查 8 个方向的边缘采样点，在笔直桥身段够用，但红蓝双方基地附近的可走区域是"桥身+圆形基地圈"融合出来的弯曲边界，弯曲处完全可能从两个采样方向的空隙里"钻"过去——用户反馈"红蓝方水晶塔处"还有冰，正是这个形状。改成整圆网格采样（`r*0.4` 格距扫描冰块的外接正方形，只留圆内的格点，全部不可走才摆），不再是几个点，覆盖任意弯曲边界。**Q2-缺口相邻的完好墙块要有损毁痕迹**——用户："断掉的这两边的墙体会有损毁痕迹"，v0.4 只在缺口本身摆瓦砾堆，紧邻缺口的完好墙块光秃秃的。新增 `_buildWeatherChips`：贴着缺口那一侧的边缘补 2~3 块嵌在墙面上的小崩块（跟缺口地上那摊瓦砾区分开——一个是"塌了"，一个是"没塌但震裂了"）。**Q4-火炬重做（两小轮）**——用户："应该是火盆承载着火焰在石墙上"，第一次改成"从墙面探出支架+火盆"，结果支架起点算成 `WALL_THICK/2`（6.5），比柱子自己的半径（12）还小，支架其实是从柱子实心内部起步的，火盆离柱心只有 19 个单位，跟柱子的雪冠（半径约 10.35）糊在一起，用户看完直接否掉这条路："那个火盆应该就是在原来的柱子上，不要挂着！"——现在拿掉支架，火盆直接贴住柱身表面（偏移量＝柱身在该高度的实际半径+火盆半径，不留缝隙），挂在柱子偏上方（雪冠下面）。**副作用**：给 `howling_abyss_frost_v1` 声明 `map.torches`（26 个石柱坐标）之后，火炬光源接上了项目已有的"地图火炬光池"（`ThreeRenderer.torchLights`/`_syncTorchLights`，`torchPoints()` "地图自己声明的优先"），不用另起一套灯光机制；但这破坏了 `sim_v46.mjs` 一条老断言（炬⑧：撒点要覆盖地图右下 40% 象限）——那条断言是给**程序化撒点**兜底的（历史上真出过"世界尺寸字段名写错，撒点全挤左上角"的 bug），嚎哭深渊声明了 `torches` 之后走的是"作者钦定位置"分支，26 个点精确贴着桥的反对角线，反对角线上任何点都不可能同时 x>0.6W 且 y>0.6H（两个条件互斥），这不是 bug 是这条对角线的几何形状决定的，把这条断言改成只对没有声明 `torches` 的地图生效。**Q3-桥面拓宽+石墙做体积碰撞边界（未落地，等用户拍板）**——用户："把桥的宽度向两边略微扩大，墙也跟着外移，把墙作为体积碰撞的边界"。这条会动到仿真层：嚎哭深渊冰封版目前**逐位复用**原图 `howling_abyss.js` 的 `HA_NAVGRID`（`sim_frostbridge.mjs` 注④断言钉着这一点），拓宽桥面等于要么改这份两张图共用的位图（原图会跟着变宽，违反"原图完全不动"的既定规则），要么给冰封版单独描一份更宽的新位图（工作量不小，且两张图从此桥形不再完全一致）；"墙作为碰撞边界"目前项目里没有独立于 navgrid 的"体积碰撞"概念，实际能做的是"墙外侧以外从可走改成不可走"，本质还是改 navgrid。`HowlingAbyssDecor.js` 开头写明"纯装饰，不改变任何占位判定"，这条会打破这个前提，需要先跟用户对齐技术方案再动手，见第 6 节新增待确认项。**用户先后两条追加反馈**：①"火盆不是贴在柱子身上，而是顶在柱子的上面！！！！"——上一版把火盆贴到了柱身侧面，改成摞在柱顶（跟雪冠同一根竖直中轴线上，不做侧向偏移），顺手删掉了不再需要的支架相关代码。②用 AskUserQuestion 确认了 Q3 的技术方案："新描一份更宽的 navgrid，只给冰封版用"（原图完全不动）。|
| v0.6 | 2026-09-05 | Q3 落地：新增 `HA_NAVGRID_FROST_WIDE`（`map_navgrids.js`）——对原 `HA_NAVGRID` 的位图做半径 3 格（≈27.25 世界单位，2325/256≈9.08 世界单位/格）的形态学膨胀，验证过是原图的严格超集（只增不减，可走格数 17549→19577，+11.6%），原图 `howling_abyss_v1` 的 navgrid 完全不受影响（`sim_frostbridge.mjs` 原④断言）。**墙/柱子偏移量改成数据驱动，不是写死的常量**：一开始想按"140+27.25≈167"这种固定值算，但实测发现手描的真实桥形本来就不是恒定半宽（中段本身有±20左右的自然起伏，两个基地附近明显更宽，v0.5 记录的实测数据：140~299 不等）——钉死一个常量会让墙在某些弧长处贴着可走区域边缘走（太紧凑，像画在桥面上），另一些地方又空出一大截（不贴边，起不到"边界"作用）。改成 `edgeOffset(d, sign)`：在模块加载时直接对着新 navgrid 沿每个缺口弧长的法线方向扫描，扫到从可走变不可走的那一格就是这段桥的真实半宽，柱子/墙紧贴这条量出来的边界摆——这样墙天然贴合每段桥自己的实际宽度，不需要另外维护一份"每个缺口该留多少偏移"的表，也不会在新 navgrid 未来再调整时悄悄跟丢（下次改 navgrid，这份偏移会自动跟着重新量一遍，不用记得手动同步）。`obstacles` 数组（缺口标记，确认过当前未被任何仿真/渲染代码消费）仍然保留在原始的 `BASE_GAP_OFF=140`，不随墙外移改变——它描述的是原始桥形本来的属性，不是这次装饰层的事。测试新增 4 条断言（超集关系、确实变宽、原图 navgrid 未变），实机截图验证过墙贴合新边界、冰块/浮冰全部不再穿模到（新的、更宽的）桥面上。|
| v0.7 | 2026-09-05 | v0.6 上线后用户带 3 张截图连续反馈，逐条排查落地：**①"逐柱子贴真实边界"导致墙扭来扭去（真 bug，不是审美偏好）**——`edgeOffset(d,sign)` 本身量得没错，但手描位图在像素级别不平滑，相邻缺口弧长量出来的真实边界会来回跳（实测同一侧中段内 217.8~359.9 之间摆动），逐柱子各自贴边导致墙一段一段拐来拐去，用户原话"你他妈的做的什么破墙，要原先那样的平直的"。改成每侧一个统一常量：排除紧邻两个基地的首尾弧长（那两处边界天然摆到 350+，会把常量拉得离桥面老远）后，对中段各弧长的真实边界取**平均值**，`WALL_OFF_LEFT`/`WALL_OFF_RIGHT` 分居左右两侧、各自一条直线。**②取平均值后墙贴着真实边界摆，第一次上线时误用了最大值，被指出"浮在水面上"**——取最大值能严格保证墙不切进可走桥面，但真实边界摆动本身有近 60 个单位的落差，用最大值当唯一常量意味着边界局部收窄处墙会明显离开水岸，用户原话"墙都他妈的浮在水面上了"。改用平均值：多数弧长处墙紧贴或接近真实边界，只有边界局部窄于平均值的少数几处墙体会比真实边界略靠内几个单位——这一点点重叠不会露出破绽（瓦砾/冰块摆放各自独立查 `isWalkable`，不会因为墙体位置的几个单位误差而滚到桥面上）。**③随机 55% 概率断墙，用户要求"必须断"（真需求，不是审美）**——用户发来带红叉+加粗红线的截图批注："我让你把桥墙断掉，你听不懂人话？？X是断的地方，红色加粗是没有桥的地方"，字面意思是这些缺口位置本身就该没有墙/没有桥，是桥的固定地理特征（呼应 `HA_TERRAIN.obstacles` 13 个固定弧长本来的设计意图），不是随机装饰。把 `WALL_GAP_CHANCE` 从 0.55 改成 1（每个缺口必断，不再有"运气不好摊到完整墙"），同时把每处缺口拆除的墙块数从 1 块加到 2 块（新增 `WALL_GAP_BLOCKS=2`）——"红色加粗"这个措辞暗示单点式的一块瓦砾（约 19 个世界单位宽）在缩小的战场视角下太窄，读不出"这里没有桥"，加宽到两块让断口视觉分量够。实机 Playwright 截图逐个缺口位置核实：柱子两侧稳定露出双倍宽度的豁口+瓦砾，桥身沿线 13×2 处全部一致，不再看运气。**附带排查**：用户截图里疑似"路面上有淡色六边形平台"的观感，逐帧核对后确认是小兵模型自身的六边形底盘贴着桥面站立时的正常显示（低模小兵的底座本来就是浅色六边形），不是装饰层混进桥面的 bug；水域里散落的六边形是浮冰/礁石装饰，全部落在不可走的水面一侧，没有伸进桥面。|| v0.8 | 2026-09-05 | **认知纠正：全桥只有 1 处"缺口"，此前三版断墙机制全建立在错误前提上。** 用户直接发来地形位图截图、红圈圈出那一处，并明确"我指的桥上的缺口是我画红圈的地方，墙在桥的两侧，正好有一块的墙会穿过桥的缺口那里，所以那部分的墙就断掉"。实测确认：`GAP_D` 那 13 个弧长只是**柱子间距**，与桥的凹口无关；沿桥细扫两侧真实边界，sign+1 侧全程无凹口（中位半宽 233），sign-1 侧只有弧长 1240~1400 一处凹进去 67 个单位（半宽 214→147），原图 `HA_NAVGRID` 里就有、v0.6 的膨胀完整保留了它。此前的 `WALL_GAP_CHANCE`（0.55→1）+ `WALL_GAP_BLOCKS`（2）整套机制连同两个常量一起**删除**，换成一句几何判据：**墙脚下没有桥的地方就不摆这块墙**（逐块从墙块中心沿 `inward` 往桥内侧探 `groundProbe`=12 采样，不采中心点——可走区域正好收到墙线上，中心点落在边界格会随取整抖动；没注入 isWalkable 时兜底按"有桥"，退化成一条完整的墙）。结果：断口有且只有一处、恰好落在那个凹口上（实算 8 块砖、弧长 1250~1390），两侧其余墙体连续不断且与柱子严丝合缝——同时满足用户这一轮的另一条要求"柱子和墙是一体的"（v0.7 每根柱子旁拆 2 块砖的做法全部撤销）。**另两条同轮反馈**：①"墙再往回收一些，现在贴着桥的边缘不好看"——墙线改成"该侧中段真实边界的**中位数**减去 `WALL_INSET`"（中位数对那一处凹口免疫，比 v0.7 的平均值稳；内收量经 AskUserQuestion 由用户选定 25），实测内收 15 以上位图锯齿就全部落到墙线外侧，墙从"毛边"变成干净直线（内收 0 时会碎出 10 段假断口）。②"墙外那圈地不能走"——用户选定"可走区域收到墙线"，于是发布版 navgrid 改成在 `HA_NAVGRID_FROST_WIDE` 基础上把桥身段超出墙线的格子削掉的第三份数据（两个基地圈按 `BASE_CIRCLE_R` 排除，否则圆形基地平台会被削成条状）；内收 25 之后墙线（208/189）与**原图**半宽（207/188）几乎重合，即可走宽度回到原图水平，并没有比改动前更窄。**新接缝 `map.visualNavgrid`**：光收边会让地面底图跟着一起缩，墙看上去依旧贴在桥最外沿——用户那条"贴着边缘不好看"其实没被解决。于是把"画多宽"和"能走多远"拆成两份数据：`TerrainLayer` 在地图声明了 `visualNavgrid` 时按它画地面（冰封版声明的是收边前的 WIDE），可走判定仍然只认 `map.navgrid`，墙外侧那圈桥沿于是看得见、走不上去。这个字段是可选的，其余地图一律没有声明、逐位不变。实机 Playwright 正俯视截图逐处核对：墙与柱子无缝、墙外有可见桥沿、断口只在那一处凹口且两端柱子仍立着。测试 55→60 条。|| v0.9 | 2026-09-05 | **阶段一：按 Thronefall 参考重做石墙**（用户发来 16 张实机截图当参考，并用 AskUserQuestion 确认了推进方式=分两阶段先墙后环境、配色方向、侵蚀口径、灰绿问题四项）。从参考里提取并经用户确认的设计语言：每张图只有 2~4 个色相 + 一个深色阴影色；地面亮、环境暗；单方向又长又硬的纯色阴影；地形是大块平面多边形两阶明暗；植被只用一种形状重复；**墙是低矮细长的一条带，规律细节靠顶部压顶石的节奏，间隔立较粗的柱子，顶边不规则锯齿，绝不是一块块砖垒的**。据此落地四条：**①每跨一体**——删掉"分块砌墙"整套（`WALL_BLOCK_LEN`=20、`WALL_STONE_SHADES`=3 两个常量一并删除），每两根柱子之间只建**一根**连续墙体，柱子是唯一的分割点；材质从"3 档深浅按坐标哈希随机错缝"简化成墙身一色 + 压顶一色（用户："目前的墙太复杂了不适配简洁风格"）。**②高度减半**——`WALL_H` 26→13（用户原话"墙体的高度降低到 50%"）；`WALL_CAP_H` 7→3 跟着压扁（7 在 13 高的墙上占比过半会喧宾夺主）；`WALL_THICK` 13→8——这一条是我自己加的：高度减半后 13×13 的方截面读起来是一根粗梁而不是栅栏，收到 8 才有"扁而矮的石栅栏"的观感，用户可否决。压顶从内缩（`WALL_CAP_INSET`=2.5）改成外探（`WALL_CAP_OVERHANG`=1.5），与参考图里压顶往外挑的做法一致。柱子高度**不动**——参考图里门楼本来就明显高于墙，2~3 倍的落差是这套风格的一部分，而且火盆位置是用户反复返工过三轮才定下的，不碰。**③顶边侵蚀**——用户在选项里明确选了"只在顶边啃出缺口"（不是整跨塌掉）：墙身保持连续，顶部压顶石按 `WALL_CAP_PITCH`=14 的节奏排一层，按坐标哈希以 `WALL_EROSION_CHANCE`=0.22 的比例缺失、留下的高度也各自抖动，顶边因此是啃过的而不是一条直线。**必须与断口区分**：整跨没有墙只发生在墙脚下真的没有桥的地方（全桥仅那一处真凹口），判据与 v0.8 完全一致没有改动。**④墙两端不超出桥面**——用户："墙略微短一些，不要超过桥面部分"，并在截图上画了两条红线标出该断的位置。根因：`GAP_D` 的首尾两个弧长（280 / 2180）落在基地圈半径 330 以内，上一版两端的墙其实压在圆形基地平台上。改成两端各补一根立在桥/基地交界处的**端柱**（`WALL_END_MARGIN`=14，即基地圈边缘再往桥内让 14），落在基地圈里的那两个原始弧长不再生成柱子；实测两侧柱子弧长变成 344~2118，全部在桥身段内，柱子数仍是 13×2=26，`map.torches` 不受影响。另外把断口里的瓦砾按 `WALL_RUBBLE_PITCH`=20 稀疏摆放——扫描步长是 6，若逐个采样点都摆，瓦砾密度会是上一版的三倍多，堆成一条碎石带，同样与简洁风格相悖（这是我看实机截图时自己发现的）。测试 60→68 条：新增墙③~墙⑦（分块整套已删除、每跨只建一根墙身、高度减半、侵蚀只作用在压顶石上、只用两个色）与端①②（所有柱子都在桥身段内、且墙确实覆盖到桥两端不是缩在中间），并把桥⑥改成"中段柱子仍逐位取自 obstacles 那组弧长"+新增桥⑥b。**阶段二（待做，已获用户确认方向）**：frost 调色板往参考图雪地那张重调（冷白/淡冰蓝亮地面 + 深藏蓝暗环境，压到 3~4 个色相）、修共享光照/色调映射把冷色压成灰绿这个老问题（改动影响所有地图，会先出三张老图的前后对比给用户确认）、空白水域补山体/装饰。|| v1.0 | 2026-09-05 | **柱墙推倒重做 + 阶段二（配色 / 色调映射 / 水域环境）全部落地。** ①**柱墙推倒重做成【柱】【墙】【柱】**：用户连发两条——"你这做的啥墙啊，我咋看不懂呢，而且对应的柱子也要变短啊""你能不能把墙完全推倒重做？？？做成【柱子】【墙】【柱子】这种。并且墙体别太宽，柱子和墙要风格统一"。上一版是"6 棱锥台柱身 + 圆锥雪冠"配"矩形墙板 + 压顶石"，两套形状语言拼在一起，柱子还是 46 高、墙只有 13 高，比例也垮了。我当时判断"参考图里门楼本来就比墙高很多所以柱子该保留"——那是门楼不是间隔柱，**判断错了**。现在柱与墙是同一套构造、只差尺寸：都是矩形体块 + 同规格压顶石（同 `CAP_H`=3、同 `CAP_OVERHANG`=1.5、同两种材质），柱 `POST_H`=22/`POST_W`=11 比墙板（13 高 / `WALL_THICK` 8→6）高一截厚一圈，柱-墙-柱的节奏一眼可读。删除 `PILLAR_H`/`PILLAR_R_TOP`/`PILLAR_R_BOT` 与圆锥雪冠（雪冠是整套里最不"矩形"的一块，留着谈不上统一），火盆/火焰改锚在柱子的压顶石上——"火盆顶在柱子上面"这个返工三轮定下的关系没变。另外顺手修了读不出"一道墙"的两个原因：压顶石原来每块缩到 0.92 留等间距的缝、再叠逐块高低抖动，整道墙成了一排碎块——改成首尾相接、去掉抖动，`WALL_EROSION_CHANCE` 0.22→0.1，于是压顶是连续的一条、只有被风化掉的几处才真断开。②**色调映射（全局，影响所有地图）**：老问题"冰和水一直发灰发绿"的根因查实是渲染器一直用 `ACESFilmicToneMapping`——那是给写实 HDR 影视流程设计的曲线，会把饱和冷色往灰/青绿压并整体去饱和，正是这套平涂高饱和美术方向最怕的。新增软编码 `CONFIG.toneMapping.{mode,exposure}`，默认改成 Khronos PBR Neutral（`NeutralToneMapping`）：同样把高光平滑滚降、但不做 ACES 那种色相偏移与去饱和。`setToneMapping(on)` 开关仍有效（开＝用配置的曲线，关＝完全不做）。已按承诺给三张老地图各截了前后对比，召唤师峡谷/扭曲丛林/嚎哭深渊都无回退。③**frost 调色板按参考重调并补齐软编码**：压到 4 个色相——亮地面 `corridorColor` `#eef4f8`→`#dce9f2`（带蓝的近白：太阳光本身偏暖，纯白被暖光一乘就发米黄，之前桥面看着像沙子就是这个原因，预先往蓝偏正好抵消）、暗环境 `groundColor` `#33566e`→`#16233d`（深藏蓝，整张图的暗部锚点）、`rockColor` `#8fa3b0`→`#5c7387`（柱墙明显暗于桥面，"看不懂"的另一半原因就是墙和地面几乎同明度）。新增 `wallCapColor`/`iceColorA`/`iceColorB`/`waterColor`/`islandColor`/`spikeColor` 六个字段，把原先散在 `HowlingAbyssDecor.js` 里硬编码的冰/水/岛颜色全部收进调色板（第 2 条铁律）。④**水域环境重做（两轮）**：第一轮只是把浮冰加密加高，用户一句"这算啥环境层次啊，做的一点都不好看"——复看确实不行。真问题是**轮廓**：所有冰块都是 `CylinderGeometry(1,.88,1,6)`，即规则六边形，尺寸/概率怎么调都改变不了"一地大小雷同的硬币"。新增 `icePrismGeo(sides, seed)`：按角度槽位逐顶点抖动半径（同槽位上下环共用系数，侧面才不扭），做出不规则冰原轮廓；再叠两轴不等比拉长（0.6~1.5）。水域分三档靠尺度差拉层次：冰脊（`RIDGE_STEP`=330/`RIDGE_CHANCE`=0.8/`RIDGE_R`=[40,132]/`RIDGE_H`=[18,38]，成坨出现、离桥 `RIDGE_MIN_DIST`=300 以外）、冰刺丛（`SPIKE_PER_RIDGE`=[9,18]、`SPIKE_R`=[2.6,6.5]、`SPIKE_H`=[16,58]，往块中心聚成丛，是参考图里辨识度最高的母题）、平浮冰（`ICE_FILL_CHANCE` 0.58→0.42 给冰脊让位，半径范围从 0.72~0.98 拉到 0.38~0.98）。每一块仍旧逐个查 `clearOfBridge`，绝不压到桥上。测试 68→79 条，新增色①~色⑥（色调映射软编码且已换走 ACES、渲染器不再写死、调色板补齐、装饰层不再硬编码、桥面比深渊亮 100+ 亮度、墙比桥面暗 60+）与环①~环③（不规则棱柱、三档尺度、冰脊避桥）。`node tests/run_all.mjs` 全量 0 失败（41 套）。|

---

## 1. 目标与范围

用户原话整理：嚎哭深渊重做成冰封风格。除了一座桥之外，其余地方是水/浮冰/
冰川/孤灵小岛（**幽灵/亡灵主题**，已用 AskUserQuestion 确认——呼应"嚎哭"这个
地图名，装饰走墓碑/幽蓝鬼火/飘散灵魂光点这类）。桥上被雪覆盖，但**保留缺口**；
桥两侧有石墙；石墙上加火炬，最好带火焰特效；光源目前可以不加（等塔光照整体
方案定了再说），如果有把握做也可以做。

范围边界（不重新论证，直接抄第 4/5 节已经定的规则）：
- **原图 `howling_abyss.js`（`howling_abyss_v1`）完全不动**，新建一张风格化副本，
  与 `demo_stylized_v1` 相对于三张老地图的关系完全一样。
- 不碰仿真层——这次是纯视觉工作，`isWalkable`/寻路/索敌/弹道等判定逻辑
  **零改动**，包括下面第 4 节要讲的"缺口"。
- 不做高低差（`HA_TERRAIN.highground: {}`，本图定稿"无高低差"，这次不推翻）。
- 光源：默认先不加真动态光源（见第 3.4 节的理由），除非确认要提前做。

---

## 2. 现状勘查（动手前必须先弄清楚的事，第 4.3 节要求过）

读了 `src/data/maps/howling_abyss.js` 全文和相关渲染代码，几条关键事实：

1. **这张图是 navgrid 驱动的，不是走廊模型**——`useNavgrid: true` + `HA_NAVGRID`
   位图，桥的形状是从真实小地图逐像素描出来的（笔直桥、中段窄两端宽），不是
   `demo_stylized_v1` 那种"折线+半宽"的走廊模型。这一点很重要，下面第 3 节
   会说明它导致现有的风格化渲染代码**目前还不支持这张图**，需要先补代码
   （不是纯换配色就能跑）。
2. **"桥上的缺口"已经是现有数据的一部分**——`HA_TERRAIN.obstacles` 数组，13 个
   弧长位置 × 桥两侧共 26 个障碍物，效果是"每隔一段把桥面掐窄一次"，对应小
   地图上那排棕色方块。**这就是用户说的"保留缺口"**——不是要新增什么，是这次
   重做视觉时**不能删掉/改动这份数据**，只需要给它配一套跟"石墙"风格匹配的
   视觉表现（见 3.2 节）。
3. **本图没有河道**（`walls: { river: false }`）、**没有高低差**（`highground: {}`）
   ——这两条这次都不推翻。
4. **本图没有基地圈走廊模型**，蓝红双方各自的"基地"是桥两端天然变宽的那一截
   （navgrid 里描出来的形状），不是额外画的圆。

---

## 3. 视觉设计方案

### 3.1 桥面：雪覆盖 + 保留缺口

桥面（现在 navgrid 里 `walk=1` 的区域）改成雪白色调，替换现有导航贴图分支
（见 4.1 节）里写死的路面色 `#2b3647`。缺口（`obstacles` 数组的 26 个点）
在视觉上表现为**贴着桥沿的小块尖角冰岩**（呼应"石墙"材质，不是雪堆——雪堆
看不出"障碍"的实体感），大小与 `obstacles` 里声明的 `r: 26` 半径对应，摆在
数据本来的位置上，不额外挪动。

### 3.2 石墙：桥两侧（v0.2 已定稿）

**不是连续一整条墙**——是一段段的，相邻两段之间用一根插进地面的石柱子连接
（用户原话）。柱子的位置**对齐现有 13 处缺口弧长**（`[280, 440, 600, 760, 920,
1080, 1240, 1400, 1560, 1720, 1880, 2040, 2180]`，桥两侧各一根，共 26 根），
两根相邻柱子之间填一段墙体。这样墙的分段节奏和"缺口"是同一套数据，视觉上
"柱子=结构支撑点，缺口=结构薄弱点"是自洽的。

技术上不走"通用 WallLayer 新增贴边窄墙带模式"这条路（见 4.2 节改动说明）——
柱子/墙段/豁口的位置本来就要对齐这 13 个已知弧长值，直接用地图自己的桥体
参数化坐标（`P(d, off)`）算出每段墙/每根柱子的位置和朝向，比通用的 isWalkable
边界扫描更直接可靠。

墙体材质：低多边形石块观感（复用现有 `WallLayer` 崖面的"碎块阶梯边"处理方式，
换一套偏冷灰白的配色，接雪/霜的顶面）。柱子比墙体略高一截，顶部是这次火炬的
挂载点（见 3.3 节）。

**缺口处的破碎效果（v0.2 已定稿，取更明显那档）**：墙/桥面在缺口位置直接
"缺一块"（豁口状，不是单纯裂纹贴图），周围散落更大块的瓦砾——豁口本身落在
柱子的位置上，视觉上读成"柱子还立着、柱子两侧的墙/桥面塌了一块"，瓦砾块用
低模石块几何随机撒在豁口附近（同样走确定性伪随机，见 3.5 节的摆放纪律）。

### 3.3 火炬：石柱子顶部（v0.2：位置从"墙顶"改成"柱子顶部"）

摆在 3.2 节那 26 根石柱子的顶部（不是沿墙均匀摆，因为墙现在是分段的，柱子
才是结构上"立得住东西"的地方）。是否每根柱子都点一盏、还是隔一根点一盏，
留到实机看效果时再定夺（先按"每根都点"做第一版，观感太密再抽稀，比反过来
省事——如果你希望现在就锁成隔一根点一盏，跟我说，我先按你说的做）。

**火焰特效**：emissive 材质 + 一小簇火焰几何（不追求逼真，风格化路线一贯做法）
+ Bloom 辉光。**这里有一个必须先处理的依赖**：第 1 节记录过的描边 bug 根因是
"线/点类 FX 几何没有排除出预渲染相机"——如果火焰特效用 `Points`/`LineSegments`
实现火星/火苗，从一开始就要把它纳入 `HUD_SPRITE_LAYER`（或它的扩展），不能等
描边真正修复那天才补，否则这批新加的火炬会立刻重演"雨变粗黑线"那个问题
（现在描边默认是关的，不会立刻炸，但迟早要开，提前做对成本更低）。

### 3.4 光源：这次先不加真动态光源（可撤销的保守选择）

用户明确把这个决定权交给了我（"你要是有把握做也行"）。技术上确实有把握做
（现成的固定大小光源池机制、`3.5`/`4.4` 节已经写过方案），但这次先不做，理由：

- 塔夜间光照重做（3.5 节）的具体分级方案还没最终拍板；如果环境火炬现在先按
  某一套方案接了真灯，后面塔光照定案后可能要跟着返工。
- 这张图目前是"探路"性质的第一张风格化老地图重做，先把造型/配色跑通、拿到
  用户反馈更重要，光照可以作为独立的小快跑（fast follow）随后补。

**如果你希望这次就把火炬做成真的会照亮地面**，跟我说一声，我按 3.5 节"少数
重要节点才配真光源"的分级方式做（这张图可以全部火炬都配，因为总数不多，
不会碰到光源池数量上限）——这条改动量不大，可以随时加回来，不影响其它部分。

### 3.5 剩余区域：水 / 浮冰 / 冰川 / 孤灵小岛（幽灵主题）

这些都落在 navgrid 判定的"不可走"区域里，**纯装饰，不改变任何占位判定**。

- **水/冰面基底**：这次**不采用**第 4.5 节设想的"线段式极简水流"——那是给
  真正流动的河/湖用的，嚎哭深渊是冰封静态水面，没有"流动"这个语义，做成
  流动线段反而不对。改成一片接近静止的浅蓝白冰面（材质层面可以比现有
  `WaterLayer` 简化，不需要法线贴图滚动 UV 那套"讲究"的实现——这也顺带避开
  了第 4.5 节记录的"线段几何必须排在描边修复之后"这条依赖，这次不需要等）。
- **浮冰/冰川（v0.2：可以多放，允许大片连成一块）**：不可走区域里散布的独立
  几何块（棱角分明、低模），高度不一、朝向随机（确定性伪随机种子摆放，不用
  真随机——与 `VegetationLayer` 现有的树木摆放同一套纪律，保证同一张地图每次
  加载画面一致、可复现）。用户明确"这种装饰不用太多，就是锦上添花的作用"——
  数量上不追求铺满，但允许若干块相邻拼接成大片浮冰群（不是必须每块都孤立），
  制造"有大有小、疏密自然"的观感，不是规则网格摆放。
- **孤灵小岛（v0.2：只放 1~2 个，不是多个）**：比浮冰更大一圈的独立小岛，幽灵/
  亡灵主题装饰——墓碑（低模石碑剪影）、幽蓝鬼火（emissive 光点，同样要注意
  3.3 节提到的描边排除问题）、飘散的灵魂光点。整张图只放 1~2 座，不要铺开，
  呼应"锦上添花"的定位——这是氛围强调点，不是要铺满水域的常规装饰。

---

## 4. 技术方案（内部实现，供参考，不是设计决策本身）

### 4.1 调色板架构：从"一张全局配色"扩展成"按地图取色"

现状：`CONFIG.stylizedVisuals` 是一份写死的全局配色（`demo_stylized_v1` 专用），
`map.visualStyle === 'stylized'` 是唯一开关，渲染代码直接读 `CONFIG.stylizedVisuals`。
如果嚎哭深渊冰封版也用这个开关，会跟 demo 撞色（拿到绿色调而不是冰蓝）。

**方案**：把 `CONFIG.stylizedVisuals` 扩展成按主题分组的字典（比如
`CONFIG.stylizedPalettes = { default: {...现在这些值...}, frost: {...新的冰封配色...} }`），
地图新增一个 `paletteId` 字段（`demo_stylized_v1` 不声明时退回 `'default'`，
逐位不变；嚎哭深渊风格化版声明 `paletteId: 'frost'`）。渲染代码从"直接读
`CONFIG.stylizedVisuals`"改成"读 `CONFIG.stylizedPalettes[map.paletteId || 'default']`"。
**这是一处必须改的现有代码**，但改动方式保证 `demo_stylized_v1` 的默认取值
和当前逐位一致（第 3 条铁律要求的"参数化默认值不变"）。

### 4.2 桥体装饰生成器（v0.2：改路线，不再走"WallLayer 新增贴边窄墙带模式"）

原方案是给通用 `WallLayer` 加一条基于 isWalkable 边界扫描的新分支。用户确认
柱子/墙段位置要对齐现有 13 个缺口弧长之后，改成更直接的做法：**新建一个
嚎哭深渊专用的桥体装饰生成器**（暂定 `HowlingBridgeLayer.js`），直接用地图
自己已有的桥体参数化坐标（`howling_abyss.js` 里的 `P(d, off)` 函数、桥中线
方向向量 `U`/`Nrm`）算出：

- 26 根石柱子的位置（对齐 13 个缺口弧长 × 两侧）；
- 相邻柱子之间的墙段（位置/长度/朝向由两根柱子的坐标直接算出）；
- 每个缺口位置的豁口 + 瓦砾（豁口就在柱子所在弧长，墙段在这一格自然断开，
  不需要额外裁剪逻辑）；
- 26 个火炬挂载点（柱子顶部）。

不走通用 `WallLayer` 的理由：柱子/墙段/豁口的位置本来就要跟这 13 个已知弧长
对齐，直接用地图自己的参数化坐标比通用的 isWalkable 边界扫描更直接、不用
多一层"扫描结果反推回弧长位置"的坐标换算，也不会牵动其它地图共用的
`WallLayer.js`（改动面更小、风险更低）。如果以后有第二张图需要同类"贴边分段
墙"效果，再考虑抽象成通用机制，现在不为假设中的需求预先设计。

### 4.3 TerrainLayer 的 navMode 分支需要补上 stylized 支持

读代码发现一个现有缺口：`buildTerrainLayer()` 的风格化配色只接在【走廊模型】
分支里（`demo_stylized_v1` 用的那条），navgrid 驱动的地图（`navMode` 分支）
现在**完全没有读 `stylized`/调色板**，路面颜色是写死的 `#2b3647`。这不是嚎哭
深渊专属的新需求，是现有风格化功能本来就没覆盖到 navgrid 地图这一类——这次
要把它补上，之后任何 navgrid 驱动的地图（包括以后召唤师峡谷/扭曲丛林的风格化
版本，它们大概率也是 navgrid）都能受益，不是嚎哭深渊单独绕一条路。

---

## 5. 明确不做/延后的事（避免范围蔓延）

- 不碰 `isWalkable`/寻路/索敌等仿真层判定（第 1 节已强调）。
- 不做真正的动态光源（除非用户改口，见 3.4 节）。
- 不做流动水面的线段几何（3.5 节已说明理由，且这条依赖尚未解除）。
- 不顺手"优化"或重构现有 `howling_abyss.js` 的其它部分（比如塔属性/光环数值）
  ——那些跟画面无关，保持原样。
- 塔/兵模型仍处于挂起状态（`VISUAL-UPGRADE-PLAN.md` 3.2 节），这次只做地形/
  环境，不牵动单位模型。

---

## 6. 待确认清单

**v0.2：全部已确认，开工。** 记录留档：
1. ~~火炬密度/位置~~ → 改成摆在 26 根柱子顶部，先按"每根都点"做第一版。
2. ~~光源~~ → 这次不加真动态光源，维持。
3. ~~孤灵小岛数量~~ → 1~2 个（不是多个），浮冰/冰川可以多、可以大片连接。
4. ~~新地图命名~~ → 按暂定方案：`howling_abyss_frost.js`，`id: 'howling_abyss_frost_v1'`，
   `label: '嚎哭深渊·冰封'`。
5. ~~技术改动方向~~ → 调色板架构扩展 + TerrainLayer navMode 补 stylized 支持这两项
   维持；WallLayer 新增分支这一项改路线，见 4.2 节新方案（专用桥体装饰生成器）。
6.（v0.2 新增）墙分段 + 柱子位置对齐现有 13 处缺口弧长——已用 AskUserQuestion 确认。
7.（v0.2 新增）缺口破碎效果取"豁口+散落瓦砾"这档（更明显）——已用 AskUserQuestion 确认。

**v0.5 新增，等用户拍板（Q3：桥面拓宽 + 石墙做体积碰撞边界）：**

8. ~~拓宽桥面这条路怎么走~~ → 已用 AskUserQuestion 确认走 (a)：新描一份更宽的
   navgrid 位图（`HA_NAVGRID_FROST_WIDE`），只给冰封版用，原图 `howling_abyss_v1`
   完全不动。v0.6 已落地，见版本记录。
9. ~~"墙作为体积碰撞边界"具体挡什么~~ → 落地方式确认为"墙外侧以外的格子从
   可走改成不可走"（即拓宽后的 navgrid 边界本身），不是另外发明一套独立的
   碰撞系统——这也是本项目寻路/索敌一直以来的判定方式（全部只认 navgrid），
   没有引入新概念。子弹/技能弹道是否单独读 navgrid 这次没有专门核实，因为
   本项目现有的弹道系统一贯跟着目标/命中判定走，不单独做"弹道穿墙"检测，
   跟老地图（三张原图）的墙体行为一致，没有因为这次改动而变化。
10. ~~光影~~ → 已落地，不用等确认：光影这半句独立于第 8/9 条的拓宽桥面/体积
    碰撞（不涉及仿真层，纯渲染标志位）。`HowlingAbyssDecor` 新增
    `setShadowLevel(level)`，接入 `ThreeRenderer.setShadowLevel` 现有的分档
    机制（`this.frostDecor?.setShadowLevel?.(lv)`，跟 `WallLayer.setShadowLevel`
    同一个模式）——石头类实体（柱子/墙体/压顶石/瓦砾/火盆）跟着全局阴影档位
    走，火焰（自发光 MeshBasicMaterial）和冰面/水面不参与（前者开阴影没意义，
    后者在场景边缘、收益小，不追加阴影贴图开销）。

---

## 7. 测试计划（沿用项目既有纪律，不是这次新加的规矩）

- 新增的纯函数逻辑（调色板取值、窄墙带的几何生成规则等）按 `tests/sim_*.mjs`
  惯例配断言，钉住行为形状（不钉具体像素值）。
- `demo_stylized_v1` 现有的风格化渲染必须逐位不受影响——回归测一遍
  `tests/sim_postfx.mjs`/相关渲染测试，确认调色板架构改动没有动到默认值。
- Playwright 实机验收：新地图能正常加载、开一局，桥面/墙体/火炬/水域装饰
  正确显示，0 条真实控制台报错。
- `node tests/run_all.mjs` 全量必须 0 失败才算完成（CLAUDE.md 硬性要求）。

---

## 执行状态

- [x] 用户确认第 6 节的待确认清单（v0.2）
- [x] 4.1 调色板架构扩展（`stylizedPalettes` + `frost` 主题配色）
- [x] 4.3 TerrainLayer navMode 补 stylized 支持
- [x] 4.2 新建 `HowlingAbyssDecor.js`（原计划名 `HowlingBridgeLayer.js`，实现时改的名）：
      26 根石柱 + 分块砌墙 + 缺口处对应损毁/瓦砾 + 火炬挂点——v0.3/v0.4 两轮返工后定稿
- [x] 3.1 桥面雪化（随 4.3 一起落地：navMode 分支按 frost 调色板的 `corridorColor` 上雪白色）
- [x] 3.3 火炬 + 火焰特效（普通 Mesh+MeshBasicMaterial，不是粒子，不踩描边 bug，不需要额外接 HUD_SPRITE_LAYER）
- [~] 3.5 水/浮冰/冰川 + 孤灵小岛×2（幽灵主题）——真 bug（冰块摆放半径算错、颜色发灰）已修，
      但用户明确要求"以后再说，先把主体做完"，**这次先按现状搁置，不再继续投入**
- [x] 新地图接入地图注册表（`howling_abyss_frost_v1`）
- [x] 测试（`tests/sim_frostbridge.mjs` 34 条）+ Playwright 实机验收 + `node tests/run_all.mjs` 全量回归（0 失败）
- [ ] 悬而未决：场景光照/色调映射把偏冷色系整体往暖/低饱和方向压，导致冰/水的蓝色实际
      渲染发灰发绿——这是渲染层既有的开放问题，不是这张图独有的 bug，改动会牵动所有地图的
      共享光照代码，按标准需要用户先拍板要不要处理，这次没有擅自动

---

# 8. 下一阶段设计稿：地形立体化 / 光照 / 描边

> **状态：设计稿，已做过代码级可行性压测，等用户拍板后实施。本节一行实现代码都还没写。**
>
> 用户 2026-09-05 定性：「这是地图先行者，奠基着以后未来地图的发展」。所以本节的
> 结论不只服务冰封版——凡是能通用的，都要求做成**通用机制 + 地图按需声明**，
> 不许塞进 `HowlingAbyssDecor.js`（那个文件按头注只服务 `paletteId==='frost'`）。
>
> 参考图存档在 `docs/refs/thronefall/`（16 张，含设计语言摘要）。用户之后又发了
> 4 张雪地/夜景实机图，但那 4 张**没有落到磁盘**（uploads 目录里查不到），所以只
> 存下了文字分析，见 8.1。以后要归档得请用户重发。

## 8.1 从新的 4 张实机图读到的东西（这 4 张没能存盘，分析必须留在这里）

四张分别是：①亮白冰原（最有用）②夜景雪地村镇（远景，能看清边界处理）③同一村镇
近景 ④夜景雪原带一条陆桥。补充/修正了 `docs/refs/thronefall/README.md` 里那份摘要：

1. **立体感只靠一个装置：很亮的平顶 + 很暗的高侧面。**
   亮白冰原那张里，冰台的顶面接近白，侧面接近深藏蓝，明度差极大。整套美术没有贴图、
   没有渐变、没有环境光遮蔽，所有的"厚度"和"层次"都来自这一个对比。
   → **我们现在的冰脊用的是两个相近的中蓝，差得太少**，这是第一版环境被用户评价
   "做的一点都不好看"的残留原因之一。崖壁侧面色要往 `groundColor` 那边压。

2. **可走区域是"有厚度的台子"，不是贴花。**
   参考图里每块陆地都是一块浮在低处地面之上的板，边缘是干脆的崖。
   → 我们现在的可走区域是**零厚度贴花**（地形底图逐 navgrid 格画在同一个平面上），
   这是与参考图差距最大的一项，比"基地是不是正圆"重要得多。**这是 8.2 的全部动机。**

3. **轮廓是长条、楔形、带尖角的不规则多边形**，还会互相叠压出台地层次。不是圆乎乎的块。

4. **地图边界是大块角面板层层叠压、向外后退**，只用 3 档明度，没有雾、没有渐隐。

5. **夜景不是"把灯关暗"**，是整体染上一层蓝紫、明度压缩，但每个形状仍然读得清清楚楚。
   见 8.3。

6. **建筑是全场最亮最跳的东西**（米白 + 深色墨线），而且用户明确说了**塔是石头的，
   不是木头**。我们现在的深棕木塔正好反着。塔重做时按这个走。

## 8.2 桥 + 两侧大陆的立体化（用户提的方案 + 我的可行性结论）

用户原话：「两侧不规则的大陆（红蓝基地）+ 一座桥（悬于水面上）」，并问桥要不要做木头。

### 8.2.1 材质：石头，不是木头

两条理由压过"暖色更跳"这一条好处：
- **会跟塔撞。** 现在的塔就是深棕木塔，桥也做木的，塔就陷进桥面里了——而塔才应该是
  最跳的。（用户后来也确认了：Thronefall 里塔是石头的。）
- **桥上已经有一整套石构**（石栏杆 + 石方柱，v1.0 用户已认可）。桥面再用木头，一个
  结构上叠两种材质，跟一直要求的"简洁"相冲。
建议：**偏中性暖的灰石**，比雪地暗一档。有 land/bridge 的材质区分，又没引入棕色、
没破坏 4 色纪律。**具体色值等塔定稿后再锁**（桥要比塔暗，塔才跳得出来）。

### 8.2.2 高度：桥与大陆**同高**，不做落差

"悬空"感不靠高度差，靠三件事：桥面有厚度、**桥下按柱子的节奏排桥墩**、桥面材质与
雪地明显不同。上面 13 根柱子、下面对应的桥墩共用同一套间距，整座桥就读成一个被
工程设计过的结构。
不做落差的理由：落差会让小兵在基地入口上下台阶，是玩法可见的变化，收益不抵风险。

### 8.2.3 ⚠️ 可行性压测：核心结论与我原计划相反

要让陆地读成"高于深渊的台子"，**深渊表面必须真的比陆地低**——这一点绕不过去：
陆地和深渊现在画在**同一个平面**上，崖壁块若从边界往下伸，会直接被同高的深渊面挡住，
读成"方块陷进地板"，不是"陆地有厚度"。

三条路，逐条压测过：

| 路线 | 结论 | 理由 |
|---|---|---|
| **(A) 用 `heightAt` 把可走区域抬起来** | **否决** | 两个原因。①`MapSystem.heightAt` 是**形状驱动**的（半平面 / 圆 / 河带强度场），表达不了"跟着 navgrid 轮廓走"，得新加一个分支。②更致命：地形网格是 **24px 一段**细分的，而 navgrid 是 **2325/256≈9px 一格**。跟着 navgrid 抬高会被量化到 24px，于是画面上会出现**两条频率不同、互相错开的锯齿**——比现在还糟。 |
| **(B) 拆成两个平面**（地形只留可走区域，深渊另起一个更低的面） | **推荐** | 这正是参考图的真实构造：陆地是浮在低处地面之上的板。改动可控，见下。 |
| **(C) 完全不动几何，在地形底图上画一条深色带假装崖壁** | 备选（廉价兜底） | 画上去的"影子"不会随视角产生视差，45° 斜视下会像贴纸。作为先行者地图不该用，但如果 (B) 遇到意外阻力，这是能立刻退回的方案。 |

**(B) 的具体改动面（已逐条核对过代码）：**
1. `buildTerrainLayer` 的 navMode 分支：不可走格子写 **alpha=0** 而不是 `groundColor`；
   材质用 **`alphaTest`（discard）而不是混合**——这样深度写入是正确的，SSAO/描边的
   法线深度预渲染不会被半透明搞乱，也没有渲染排序问题。**必须做成按地图声明开启**
   （新字段），否则三张老地图会一起变透明。
2. 新增一个**更低的深渊平面**。不要复用 `MapSkirtLayer`：它是 3 倍地图边长、带
   fadeAlpha 着色器补丁、`MeshBasicMaterial` 不吃光照的氛围层，职责不同。
3. 冰原/浮冰/冰刺（`_buildWaterDecor`）整体下沉到深渊层高度。
4. 沿可走边界摆崖壁块，从深渊层高度伸到陆地面。

**已确认不受影响的**：点选/拖拽用的是"射线打 y=0 数学平面"（`ThreeCameraController`），
不是打地形网格，所以地形挖空不影响交互；`WaterLayer` 只在有河道的地图生效，冰封版
`walls:{river:false}` 用不到。

### 8.2.4 崖壁还顺带修掉一个现存瑕疵

现在桥/基地的边缘是**逐格最近邻画出来的锯齿台阶**（截图里肉眼可见），参考图里所有
边缘都是干净的斜切直线。崖壁块沿**平滑过的轮廓线**摆，正好把这圈锯齿盖掉。
⚠️ 反过来说：崖壁**绝不能逐 navgrid 格摆**，那样只会把锯齿放大成锯齿墙。

### 8.2.5 架构：这一套必须做成通用件（"地图先行者"的要求）

| 新模块 | 职责 | 为什么放这 |
|---|---|---|
| `src/data/navOutline.js`（新） | 纯函数：navgrid 位图 → 边界多边形 → 简化/平滑后的折线（marching squares + Douglas–Peucker） | 无 DOM/three 依赖，可 headless 跑断言，与 `navgrid.js` 同一层的定位。**这是最该通用化的一块**——以后每张图要崖壁都从这里拿轮廓 |
| `src/presentation/TerrainEdgeLayer.js`（新） | 通用层：读 mapSystem + 上面的轮廓，生成崖壁块 | 通用机制，地图按需声明参数（高度/顶色/侧色/块长/抖动）才启用。**不许写进 `HowlingAbyssDecor.js`**，那个文件只服务 frost |

**性能**：边界总长粗算 ≈ 9000 世界单位（桥两侧 ≈ 2460×2 + 两个基地圈 ≈ 4150），
按 40~45 单位一块 ≈ 200 块，叠一层 ≈ 400 个 mesh，叠在现有几百个冰块之上会很吃 draw call。
**`vendor/BufferGeometryUtils.js` 已经在仓库里，`mergeGeometries` 可用**——崖壁是完全
静态的，建完直接合并成 1~2 个 mesh，draw call 回到个位数。这一条要写进实现要求里。

### 8.2.6 落地顺序（按"风险从低到高"）

| # | 做什么 | 风险 | 收益 |
|---|---|---|---|
| 1 | 崖壁 + 深渊面下沉（(B) 路线 + `navOutline` + `TerrainEdgeLayer`） | 中（动共享渲染，但按地图声明开启，老图不受影响） | **最大**，顺带修锯齿 |
| 2 | 桥墩（桥下按柱子节奏排，纯装饰） | 零 | 大，"悬空"感全靠它 |
| 3 | 桥面材质换石头、比雪地暗一档 | 零 | 中 |
| 4 | 不规则大陆（程序化扰动基地圆半径、改 navgrid 两端） | **中高**（唯一动玩法可走区域的一项） | 中 |

第 4 项建议**等前三项做完看效果再决定**——很可能做完崖壁就够了。
真要做，必须验证：所有建筑坐标 + 兵线中线在新形状内留足余量；`baseCircleRadius`
驱动的阵营染色圆是**径向渐变的软光晕**，配不规则轮廓不会露馅（这一条已确认，不是阻塞项）。

## 8.3 光照（用户："目前我们的光照太烂了"）

用户对参考图的描述："亮了一部分区域，没什么特殊的，然后这个光照还比较暗但是可以看清"。

拆开看，这句话对应四件具体的事：

1. **暗但看得清 ⇒ 环境光占比要高。** `setLighting` 里已有 `ambientShare` 这个旋钮
   （总辐照度在 `hemi` 与 `sun` 之间的分配）。参考图里背光面清清楚楚、阴影是"深但不黑"，
   对应的是**高 ambientShare**。我们现在偏低，所以背光面糊死、画面脏。
2. **夜晚是"染色 + 压缩明度"，不是"关灯"。** 参考图夜景整体蓝紫、但每个形状都读得出。
   应该靠 `unitTint`/`background`/`ambientSky` 整体染色 + 轻微降 `exposure`，
   **不是**把 `sun.intensity` 压下去。
3. **太阳色不要太暖。** 之前实机里近白的桥面被暖阳一乘就发米黄（v1.0 已用"预先往蓝偏"
   的调色板补偿过一次）——治标。治本是让 key light 接近中性，色温交给整体染色去表达。
4. **⚠️ 最可疑的一条：Bloom 和 SSAO 应该关掉或压到很低。**
   参考图里既没有辉光光晕、也没有角落里的环境光遮蔽脏影——它就是平涂 + 一盏方向光 +
   一块纯色硬阴影。而我们 `ssaoOn` / `bloomOn` **默认都是 true**。SSAO 打在平涂低模上
   会在每条折缝里糊出一圈脏灰，这跟"平涂高饱和"的方向直接对冲。
   **这一条是猜想，未验证**——验证方法很便宜：运行时逐个关掉 SSAO / Bloom 各截一张对比。
   实施前必须先做这个验证，不要直接改默认值。
5. **"亮了一部分区域"** 这个机制我们已经有了：`ThreeRenderer.torchLights`/`_syncTorchLights`
   的火炬光池（冰封版 26 根柱子已经接上）。方向是对的，缺的是让它成为**唯一**的局部
   光源，底子更暗更平。

**阴影**：参考图的影子是**单方向、又长又硬、纯色无渐变**。实施时要往"硬"调（缩小
阴影贴图的软化半径），而不是继续加软阴影。

## 8.4 描边 bug（用户："先把目前的描边 bug 修了吧，thronefall 里的描边还是挺重的"）

**现状**：`ThreeRenderer` 里 `this.outlineOn = false`——v51.28 用户报了画面 bug 之后
临时禁用的，根因一直没查。`createOutlinePass`（`PostFX.js`）本身没删，`setOutline(true)`
仍能手动开。

**根因怀疑（未验证，但线索很硬）**：
`NormalDepthPrepass.render()` 的做法是 `scene.overrideMaterial = MeshNormalMaterial`
然后整场景渲一遍。three 的 `overrideMaterial` 会替换**所有**可渲染对象的材质，
**包括 `Points` 和 `LineSegments`**。而场景里确实有这两类：
- `src/presentation/WeatherLayer.js`：雨用 `LineSegments`、雪用 `Points`
- `src/presentation/UnitMeshFactory.js`：水晶粒子用 `Points`

粒子被套上 `MeshNormalMaterial` 之后，会在**满屏散布的位置写入深度和垃圾法线**，
描边 Pass 的边缘检测就会在每一个粒子上触发 → 全屏麻点。冰封版本来就在下雪，
一开描边必然踩到。
另一条独立佐证：`HowlingAbyssDecor.js` 文件头注早就写着"第 1 节记录过的描边 bug
根因就是线/点类几何没排除出预渲染相机"——跟这个怀疑对上了。

**修法（沿用仓库里已有的先例）**：`PostFX.js` 已经有 `HUD_SPRITE_LAYER` 这个机制——
血条/护盾图标那类"永远画在最上层"的精灵就是靠它在预渲染时被
`camera.layers.disable(HUD_SPRITE_LAYER)` 排除掉的。照抄这个模式：
1. `PostFX.js` 新增 `FX_PARTICLE_LAYER` 常量；
2. `WeatherLayer` 的雨/雪、`UnitMeshFactory` 的水晶粒子都设到这一层；
3. `NormalDepthPrepass.render()` 里连同 `HUD_SPRITE_LAYER` 一起 disable；
4. 再把 `outlineOn` 默认改回 `true`，并按参考图把 `outlineStrength`（现 0.85）
   往**重**了调——用户明确说"thronefall 里的描边还是挺重的"。

### ✅ 已实施（2026-09-05），根因确诊，证据链如下

验证做了，但**结论跟预想的验证路径不一样，差点被误导**，记下来：
按计划先截了"描边开+有天气"和"描边开+关天气"两张，结果**两张里的黑点在完全相同的
位置**——按原来的判据（"只有下雪时才有麻点"）会得出"跟粒子无关"的错误结论。

改用像素差分才定死：把"描边开/关"两张逐像素比对，水面上那些黑点**只在描边开启时
出现**（水色 (2,29,66) → 近黑 (4,2,18)），描边关闭时那个位置根本没有东西，
所以它们是描边 Pass 造出来的、不是真实几何。

位置为什么固定也查清楚了：`WeatherLayer.setEnabled(false)` **只把材质 opacity 设成 0，
粒子对象仍然留在场景里**，只是停止移动——所以"关天气"前后黑点在同一位置。
而 `overrideMaterial` 无视原材质透明度，opacity=0 的粒子在法线深度图里照样是
完全不透明的。三条证据合起来根因成立。

**修法（已落地）**：`PostFX.js` 新增 `FX_PARTICLE_LAYER = 2`，雨(LineSegments)/
雪·尘(Points)/雾(半透明薄纱)/水晶粒子(Points) 全部 `layers.set` 到这一层；
`NormalDepthPrepass.render()` 里连同 `HUD_SPRITE_LAYER` 一起临时 disable、渲完恢复；
主相机 `layers.enable(FX_PARTICLE_LAYER)`（⚠️ 这一句不能漏——`layers.set` 会把对象
从默认的 0 层挪走，不 enable 的话粒子在**正式渲染**里也会一起消失）。
描边随之默认重新开启，观感参数收进 `CONFIG.outline`（颜色/强度/线宽/两个阈值），
按用户"描边还是挺重的"把 strength 0.85→1.0、lineWidth 1.6→1.8、颜色由暖黑
`#1a1410` 改冷黑 `#141a22`（整套美术已经转冷）。
实机复验：黑麻点消失；运行时确认粒子层掩码=4、相机掩码=7（0|1|2 全开），
粒子在正式渲染里正常显示。

## 8.5 待用户拍板的事项

1. 8.2 的落地顺序（1→2→3，第 4 项另说）是否照做。
2. 8.2.3 走 (B) 两个平面这条路是否认可（它要动共享的 `TerrainLayer`，虽然按地图声明开启）。
3. 光照与描边的两条"先验证再改"（8.3 第 4 条、8.4）什么时候做——建议跟塔的重做放在
   一起，因为它们都直接决定"塔跳不跳得出来"。
4. 塔/兵模型 vs 剩下三张地图的先后（我的建议：**塔先做**，理由见对话记录——单位是常量、
   地图是变量，地图配色本来就是为了让塔和兵能读出来才定的；反过来改不会作废塔的造型）。

---

---

## 9. 塔模型：v52/v53 两轮重做**已回滚**，教训记在这里

> **结论先写：v52（红蓝两套造型语言）与 v53（收分斜壁 + 内核外壳 + 火盆水晶）
> 两轮全部被用户否掉并回滚。** 当前塔模型 = v51 那一版（`eeddac5`），
> 只把石色调亮了一档。下面这一节留的是**为什么会连输两轮**，不是设计方案。

### 9.1 需求是怎么一路变的（时间顺序）

1. 「三档模型（正常/轻损/重损）的设计要保留，本轮只做正常模型。」
2. 「太丑了…可以完全推翻现有的设计！**红/蓝方的设计风格应完全不同**！」→ 做了 v52。
3. 「蓝方上面那一堆柱子太丑了，红方的简直都没法看。**蓝方方正/秩序，红方圆滑/混沌**。」
4. 用户发来 15 张实机图，随后改口径：
   「**目前不要求红蓝方分开设计了，只做一种风格的就行**，但这种必须要做好」
   「轻损是 33% **主体结构**破坏（高度不变），重损 67%（破烂不堪摇摇欲坠）」→ 做了 v53。
5. 「你这做的啥玩意啊，**都不如你没做之前那版本的**。」
6. 「**你现在不许做那么多**！只能做外塔/内塔/水晶塔/枢纽塔的**正常模型**，
   不许多做！！！**给我参考你没做之前的那种**！」

### 9.2 我连输两轮的真实原因（不是"审美不同"）

**① 每一轮都在推翻整座塔，而不是在既有那版上改一处。**
用户从头到尾抱怨的是**具体的点**（太暗、顶部太碎、四档看不出差别），
我每次都升级成"整体重做"，于是每次都把原本还行的部分一起弄没了。
第 5 条那句"不如你没做之前的"就是这么来的。

**② 对照图的取景比例是错的，我拿它当依据调了整整一轮参数。**
`tests/browser/tower_sheet.html` 原来按每座塔自己的 `topY` 算正交半宽，
**每一格都把模型撑满** —— 四个档次在图上一样大。我据此判断"档次差异不够"，
然后去加高、加宽、加段数，全是白做。真正的问题在渲染工具，不在模型。
（已修：`SHEET_D` 固定全表比例。这条改动**留下来了**，是这两轮唯一的正资产。）

**③ 参考图给的是"语法"，我读成了"照抄某一座塔"。**
用户明说「不是让你照着做」。我却把收分斜壁、火盆、细针一次性全塞进去，
结果既不像参考、也不如原版。

### 9.3 当前状态（本次实际交付的东西）

- 塔模型代码回滚到 `eeddac5` 的 `UnitMeshFactory.js`；`sim_v44` / `sim_v46` 一并回滚；
  v52 新增的 `tests/sim_towermodel.mjs` 删除、`run_all.mjs` 注销。
- **唯一保留的模型改动**：`FACTION_STYLE` 石色调亮一档
  （blue `#8e9aa8`→`#b9c6d6`、red `#9a8478`→`#c9b2a4`，trim 同步提亮）。
  针对的是用户最早提的那条：塔比地面还暗、沉进地里。
- **唯一保留的工具改动**：对照图统一取景比例（见 9.2 ②）。

### 9.4 下次动塔模型之前先读这几条

- 用户说"重做"时，先问清楚**是重做哪一部分**；默认按"只改被点名的那一处"办。
- 改造型之前先确认**验收用的图是可信的**（比例、取景、光照），否则会照着错误的图调参数。
- 一次只改一件事，改完立刻出图给用户看，不要攒一大堆再交付。

### 9.5 清单（明确没做的）

- [ ] 轻损 / 重损的真正造型（用户要求：33% / 67% **主体结构**破坏，轻损高度不变）。
- [ ] 召唤水晶 / 水晶枢纽的造型（用户：这两个**只有一级**，不需要升级序列）。
- [ ] 废墟（ruin）分支仍是 v44 那套乱石堆。
- [ ] 森林主题地图的"大片空地"处理（密集小树丛 + 地面麻点 + 悬浮小菱形 + 扁平小岩）——
      用户明确说等做到森林主题地图时再用，当前是冰封主题。

---

## 10. 塔/小兵与地形的割裂感：根因诊断（v54）

用户原话：「最严重的问题是，目前的塔模型**根本无法融入地形**……thronefall 里面的塔就是很明显
和环境可以融为一体，我们目前这个割裂感太严重了……**最重要的就是塔/小兵和环境的割裂感**！」

### 10.1 这不是"模型丑不丑"的问题

先说结论，因为这条直接解释了我前面几轮为什么白折腾：
**割裂感的根因在渲染架构，不在塔的造型。** 在错误的层面上改造型，改多少轮都不会好。

查到的事实（逐条可复现）：

| # | 事实 | 位置 |
|---|---|---|
| 1 | 地面 = `PlaneGeometry` + `MeshLambertMaterial({ map: 离屏画布贴图 })`，**是一张画出来的平面** | `ThreeRenderer.js` 的 `_rebuildTerrain` |
| 2 | 冰封图没有高度抬升：`MapSystem.heightAt` 在 `!this.hasWalls?.()` 时直接 `return 0` | `MapSystem.js` |
| 3 | 塔/小兵 = 真 3D 几何 + `MeshStandardMaterial`（顶点色，roughness 1） | `UnitMeshFactory.unitMaterial` |
| 4 | 默认画质 medium → `shadow: 'static'` → `unitsCastShadow()` 只在 `'all'` 为真 → **小兵完全不投影** | `ThreeRenderer.unitsCastShadow` |
| 5 | 太阳仰角写死 55°，影子短且压在物体脚下 | `SUN_ELEV_DEG` |
| 6 | 描边三档全关（v51.28 因 bug 关的），而那个 bug 已在 `f83a3c1` 修好、`CONFIG.outline.enabled` 也已是 true —— **修好了但没开回来** | `CONFIG.ui.qualityPresets` |
| 7 | 塔脚没有任何接地暗部（无 AO 贴片、无 decal） | — |

### 10.2 为什么这几条加起来就是"割裂"

Thronefall 的一体感只靠三件事：

1. **所有东西都是同一种材质语言的 3D 几何**（崖、树、房子都是同一套平面着色的低面体）；
2. **同一根黑描边**把所有轮廓勾在一起 —— 这是最强的"缝合线"；
3. **长而平的硬投影**，把每个物体钉在地上。

我们这边：地面是"画"的、单位是"3D"的（第 1 条断）；描边关着（第 2 条断）；
小兵没影子、塔的影子又短（第 3 条断）。**三条全断**，所以无论塔长成什么样都会像贴纸。

### 10.3 方案与优先级

第一梯队（纯渲染层，不动模型几何，出问题好退）：

- **A1 把描边开回来** —— `qualityPresets` 的 medium/high 改回 `outline: true`。
  成本近乎零，是性价比最高的一条。注意它的边界：描边通道是整场景 `overrideMaterial`，
  地面是**平面**，所以只会在地图边缘产生线；它能缝合"单位与单位""单位与地面"的轮廓，
  但**不会**让地面自己长出结构线 —— 那要等 B1。
- **A2 太阳压低** —— `SUN_ELEV_DEG` 55° → 32°，影子拉长。软编码进 `CONFIG.ui.lighting`。
- **A4 接地暗斑** —— 每个单位脚下一片跟随的暗色贴片。它**不吃阴影贴图开销**，
  所以即使玩家把阴影档关到 off 也仍然接地。这是低成本的保底手段。

第一梯队里需要先测再定的：

- **A3 小兵也投影** —— 放宽 `unitsCastShadow()`。塔 + 小兵一起进阴影贴图，开销要实测帧时再定，
  不能拍脑袋打开（这条如果盲开，低端机会直接掉档，而掉档又会把描边一起关掉，得不偿失）。

第二梯队（根治，工作量大）：

- **B1 地面变成真几何**（崖边侧壁 + 下沉深渊面）。路线在 §8.2 已经论证过（走路线 B：
  两个平面 + `alphaTest` 裁切，不走 `heightAt`，理由是 24px 网格与 9px navgrid 的分辨率错位
  会做出两道对不齐的台阶）。**地面不变成 3D，一体感最多只能做到七成。**

第三梯队：

- **C1 统一调色** —— 塔的石色从全局灰改成按地图调色板取（冰封取冰蓝灰）。

### 10.4 顺带处理的两件（用户点名）

- **D1** 删掉塔顶那堆小块：蓝方的**方齿雉堞**与 `TIER_SPEC.turrets` 那圈**角楼**。
- **D2** 水晶下面加一个**不显眼**的小底座（用户："不需要太显眼"）。

---

## 11. 陆地厚度落地（v55）：崖壁 + 下沉深渊面

用户：「别忘了我上次说的**两方基地变成不规则的大陆，中间用桥连接**！应该**大陆和桥要做出立体感**！
就像是你做旁边那个冰的装饰一样！」

§8.2 的路线 B 这次真正落地了。分两半：**厚度**（本节，已做）与**不规则形状**（§11.4，待做）。

### 11.1 两个新通用件

| 模块 | 职责 |
|---|---|
| `src/data/navOutline.js` | 纯函数：navgrid 位图 → 边界环 → Douglas–Peucker 简化 → Chaikin 圆角 → **世界坐标**闭合折线。无 DOM / three 依赖，可无头跑断言 |
| `src/presentation/TerrainEdgeLayer.js` | 通用层：读地图声明的 `map.terrainEdge`，铺一张下沉的深渊面 + 沿轮廓摆一圈崖壁，合并成 2 个 mesh |

按 §8.2.5 的要求，**没有写进 `HowlingAbyssDecor.js`**（那个文件只服务 frost）。
任何地图声明 `terrainEdge` 就能用，参数（崖高/下沉/块长/抖动/配色）全在声明里。

### 11.2 三个踩到的坑（都写进注释和断言了）

**坑一：轮廓不能"沿格子走"。**
第一版用 Moore 邻域跟踪，环在细颈处会**沿原路折返**，同一段边走两遍；折返段的弦长为 0，
Douglas–Peucker 对它无能为力 —— eps 从 1.2 调到 4.0，顶点只从 16264 掉到 13166，**等于没简化**。
改成「先收集内外分界的**单位边**（格与格之间的缝），再把边串成环」之后，
每条缝只存在一次，1116 个原始顶点 DP 后剩 17 个，周长 7782（与手算的 ~8000 吻合）。

**坑二：闭合环做 DP，接缝点不能随便取。**
DP 恒定保留首尾两点，而首尾就是接缝。接缝落在直边中间时，**紧挨着它的那个真角会被并进闭合段里丢掉**。
实测：320×240 的矩形，顶点数对（4 个）、包围盒也对，但第一个点是 `[170,200]` 而不是角点
`[160,200]`，周长 1110 而不是 1120 —— 角被切了。
修法：先把环旋到「离质心最远的点」当接缝，那个点必定在凸包上、必定是角，DP 一定保留它。

**坑三：`map.world` 是 `{ w, h }` 不是数字。**
直接 `world / n` 得到 `NaN`，而 `NaN` 一路传到几何里，表现是「崖壁一块都没有」而**不是报错**。
静默失败最难查，所以显式兼容两种形状，并且有断言钉住「不许算出 NaN」。

### 11.3 已确认的实机效果与参数

`abyssDrop: 24 / cliffHeight: 18`（`abyssDrop` 必须 > `cliffHeight`，否则崖壁踩不到底会悬空）。

- 桥读成**浮在深渊之上的板**，边缘有厚度和亮色崖顶；
- 长影（v54 把太阳仰角上限压到 34° 之后）落在深渊里，深度感是真的；
- 水域装饰（浮冰/冰刺）**整体下沉到深渊面那一层** —— 不沉的话浮冰会停在陆地高度，
  等于悬在水面上方 `abyssDrop` 那么高。这是 §8.2 改动面的第 3 条。

⚠️ 试过 `abyssDrop: 44`：深渊太深 + 长影，浮冰整个沉进暗部看不见了，反而丢了一层内容。
24 是「有明确深度、又不把水面内容吃掉」的值。

### 11.4 不规则大陆（已做）

用户口径：「**也改可走区域**，但是不要影响游戏平衡，就是**可走区域面积不要差太多**」；
「**桥不要收窄**」。

做法：基地那两个圆换成极坐标下的低阶谐波叠加 `r(θ) = R·s·(1 + Σ aₖ·sin(kθ+φₖ))`。

- **只用 2/3/5 三个低阶谐波**。阶数再高就变成"锯齿圆"而不是"大陆"，
  而且高频起伏落到 9 单位一格的 navgrid 上会被量化成毛刺。
- **参数写死、无随机**。navgrid 是编译期常量，随机会让每次构建出的地图不同。
- **桥体一格不动**（用户："桥不要收窄"）。

⚠️ **面积不能靠"等圆面积"归一就完事。**
原来的基地区并不是标准圆 —— 它还带着与桥交接处的喇叭口，实测比 πR² 大。
直接换成等圆面积的不规则形，可走格数掉 6%（实测 18427 → 17319）。
所以最终是**用原图实测的格数当目标、二分出一个整体放大系数**：
结果 18426（差 0.01%），全图连通、无孤岛、12 座建筑与兵线路点全部仍在可走格上。
这样即使以后有人改了底图或桥宽，面积也会自己对齐，不会悄悄漂走。

### 11.5 遗留：地图边界的矩形色阶（未解决）

地图边界处仍有一条肉眼可见的**矩形色阶**：界内 (2,22,50)、界外裙边 (4,36,76)。

我一度判断界内那片暗区就是本层铺的深渊面，于是把 `abyssColor` 按实测比例提亮 ——
**画面毫无变化**。说明那片暗区另有来源（疑似 `MapSkirtLayer` 的中心透明区，
或自动雾把远处压暗），不是这一层。

**结论先记在这里，下次不要再照着"调深渊色"这条错路走一遍。**
真要修，第一步是确认那片像素到底由哪个 mesh 画出来（把各层逐个隐藏二分），
而不是继续调颜色。

---

## 12. 一条边界，三处共用（v55.1）—— "条在乱飘"与"锯齿感太重"是同一个 bug

用户反馈两条，我一开始以为是两件事：

> 「你新做的那个条在已经乱套了，在图上乱飘」
> 「目前大陆的锯齿感太重了，我想让主地形也像你新做的装饰那样，在边缘没有锯齿感」

**它们是同一个根因**：v55 让"陆地边界"这一个概念在工程里存了**三份**，量化粒度还各不相同。

| 用途 | 数据来源 | 粒度 |
|---|---|---|
| 能不能走 | `map.navgrid` | 256 格 = **9.08** 世界单位/格 |
| 地面底图画多大 | `WallLayer` 的采样网格（`CELL = 8`），从 256 格**最近邻重采样** | 8 世界单位 |
| 崖壁摆在哪 | `navOutline` 追边 → DP(2.2 格 ≈ 20 单位) → Chaikin | 平滑折线 |

9.08 与 8 是两次频率不同的量化，**会打出拍频**——所以锯齿不只是台阶，还叠了一层摩尔纹，
比单纯的格子边更难看。而平滑折线与那条台阶最大偏离 23 世界单位，崖壁块厚度只有 16、
朝内偏移 8，**偏离超过 8 就整块脱离地面**。

### 12.1 量出来的证据（不是看图猜的）

沿平滑轮廓密采样 2620 点，逐点去问"这里的地面底图到底画没画"：

```
落在已画地面内 1845 / 落在挖空区（脚下是空的）775
→ 悬空比例 29.6%
轮廓顶点到底图真实边界的偏离：平均 5.0，最大 23 世界单位
```

修复前更糟（那时 `traceLoops` 还带着下面 12.2 那个 bug）：悬空 **50.8%**，最大偏离 56。

### 12.2 顺带查出来的真 bug：`traceLoops` 会把边界环串烧

初版用 `Map<起点, 单条出边>` 存邻接。但**一个格点可能是两条边界边的起点**——
两块地在对角相接（十字细颈）时，公共的那个格点就有两条出边。初版把第二条塞进
数组的 `.alt` 字段，而串环的循环**从来没读过 `.alt`**：边被静默丢掉，链条于是从
一条环的中间窜到另一条环上。另外 `used` 标记的是**点**不是**边**，第二条环刚起步
就会撞上"已用"而中断。

后果（冰封图实测）：应有 1 条环（两块大陆经桥连成一体），实际串出 **13 条**，
其中 8 条是零面积的横向碎片，环面积代数和 **−4275**，而正确值必须等于可走格数 **20113**。

**这类错误顶点数/环数断言完全钉不住，只有面积恒等式能。** 于是加了：

```
Σ signedArea(环) == 位图里 1 的格数
```

在构造的"对角相接两方块"和真实地图上各钉一次（`sim_navoutline.mjs` 环①②③）。

修法：邻接改成**多重映射**（一个起点挂一串出边），消费的是**边**不是点；
十字细颈处固定取**最右转**（叉积最小）——在"内部在左手边"的绕向下，最右转让每块地
各自闭合成一环，取最左转会把两块串成一个 8 字、面积互相抵消。

> 注意绕向：修好之后外环的 `signedArea2` 是**负**的（格坐标 y 向下）。
> 老注释里"外环逆时针、为正"的说法是错的，已改。填充一律用 even-odd，不依赖符号。

### 12.3 第二个坑：Chaikin 把地图边界的直角磨圆了，啃掉了两个基地的角

两块大陆在地图的两个角上，被地图边界**直接裁断**。那条边不是地形边缘，是地图自身的
裁剪线，Chaikin 却照样把它和真实地形一视同仁地磨圆，结果 **417 个可走格落到多边形外**——
地面照多边形填的话，那些地方会被挖空，**小兵会走在空气上**，而且正好在两个基地里。

诊断过程值得记：我先试了"把位图膨胀 1~3 格再追边"，417 → 403，**几乎没变**。
正是这个"调参数不起作用"说明漏的不是边界精度，是成片漏的。把漏掉的格子打成 32×32
的字符图，一眼看出全部堆在左下和右上两个角上，才定位到 Chaikin。

修法：`chaikinClosed` 加 `locked` 谓词，贴着地图边界（`x≈0 / x≈n / y≈0 / y≈n`）的顶点
**原样保留**，不参与磨圆。改完 417 → **0**。

顺带：`chaikinClosed` 从"逐边产出两点"改写成"逐顶点产出它的替代点"。两者在无锁定时
逐位等价（顶点 a 被前一条边的 3/4 点和后一条边的 1/4 点取代），但只有按顶点写才能
对单个顶点做锁定。

### 12.4 真正的修法：取消另外两份，不是调参数

新增 `navOutline.mapOutline(map)` —— **全工程唯一的一条陆地边界**，带按 `map.id` 的缓存。

* `TerrainLayer`（画地面底图）：声明了 `terrainEdge` 的地图**不再逐格填色**，
  改成 `ctx.fill('evenodd')` 直接填这条折线；陆地以外 `clearRect` 成 alpha=0。
* `TerrainEdgeLayer`（摆崖壁）：删掉自己那份 `navOutline` 调用，改读 `mapOutline`。
* 简化容差 / 平滑遍数的默认值收进 `OUTLINE_DEF`，只此一份；地图可在
  `terrainEdge.simplifyCells / .smoothPasses` 里覆写。

两者从此共用**同一组顶点**，物理上不可能再错开。矢量填充的边缘精度 ≈ 1 个底图像素
≈ 2 世界单位（底图是 0.5 分辨率烘焙），原来是 8~9 世界单位的台阶——锯齿一并消失。

几个必须注意的点：

* **only-if-declared**：三张老地图没有 `terrainEdge`，仍走逐格填色那条路径，逐位不变
  （断言 共⑩ 专门守着那段 `createImageData` 不被删掉）。
* **河道与阵营底色要 `clip('evenodd')` 裁到陆地内**。挖空区的残留 alpha 虽然低于
  `alphaTest 0.5` 会被 discard，但不要指望阈值兜底——换个阈值就翻车。
* `invalidateTerrainCache` 原来**漏删了 `#cut` 这两个 key**（地图编辑器改地形后
  拿到的还是旧画布），一并补上，并连轮廓缓存一起丢。
* `alphaTest` 是硬阈值，canvas 填充的抗锯齿半透明边会被砍在 0.5 处——正好落在真实
  多边形边界上，没有光晕，是想要的行为。

### 12.5 GPT 那轮评估里，哪些采信了、哪些是错的

用户把这两个问题拿去问了 GPT。它的判断**大方向对**，但有两处技术前提是错的，记下来：

* ✅ **根因不是"地面是 2D Plane"，现在不该把地形整体 3D 化。** 与 §8.2.3 的结论一致
  （理由不同但更硬：地形网格 24px、navgrid 9px，跟着抬会量化出两条错开的锯齿——
  正是本节这个坑的另一个版本）。
* ✅ 墙太高（18）太薄、装饰喧宾夺主、结构色接近黑、塔缺地基、应有统一 `WORLD_PALETTE`。
* ❌ 它建议"从现有 boundary loop 直接生成 TerrainEdge"——**本来就是这么做的**，
  问题恰恰出在这儿。它列的 A~H 里只有 E（三处共用同一条边界）是真问题，其余都建立在 E 之上。
* ❌ 它说"似乎把同一套 wall system 同时拿去做陆地外缘和桥侧边"——**不是**。桥侧的柱+墙在
  `HowlingAbyssDecor._buildPillarsAndTorches / _buildWallSegments`，与 `TerrainEdgeLayer`
  从来是两套代码。观感结论（桥侧护栏太高）成立，技术前提错的。
* ⚠️ 它给的落差（陆地 0 / 边缘底 −4~−8 / 水面 −8~−12）偏保守：45° 正交下落差 `h` 的
  屏幕垂直位移只有 `h·sin45° ≈ 0.71h`，落差 6 大约 4 像素，再乘视图缩放基本看不见。
  用户拍板：**落差 12 / 水面 −20**。

### 12.6 本节没做的（用户明确要求分次交付）

* 崖壁从"高瘦竖直墙"改成"矮厚外倾岸坡"（落差 12 / 水面 −20）——这才解决
  "45° 下背向相机的那两条边看不见崖壁"的老问题。
* 桥侧护栏降矮加厚，与陆地边缘共享调色板但不共享几何。
* 塔底加低矮地基（颜色取地面色深一档）。
* 统一 `WORLD_PALETTE`；flat shading。
* §11.5 的地图边界矩形色阶仍未解决（本次实机截图里还在）。

---

## 13. 世界融合第一/二轮（v55.2）：岸坡 + 桥墙降权重 + 塔基

外部评审（用户拿去问 GPT）与用户自己的判断汇到同一句话上：

> 问题不是"墙够不够像石头"，而是**陆地边缘到底是不是地形的一部分**。
> 现在的画面读成"地面 Plane → 独立墙体 → 独立桥体 → 独立塔体"的拼装感。

这一节记这两轮实际做了什么、量到了什么、以及**我在过程中连着两次判断错了**。

### 13.1 先说两次判断错误（都是同一类，值得记）

**① 岸坡从头到尾没进过场景，我却对着截图说"岸坡出来了"。**
改写 `TerrainEdgeLayer` 的几何时，我把旧的竖直崖壁那一段整体替换掉，
`addMerged` 这个"合并 + 加进 group"的辅助函数**连带被删了**——新代码算出了
`slopeGeos/edgeGeos` 然后原地丢弃。而我看截图时把 `visualNavgrid` 那圈
"看得见但走不上去的檐"当成了岸坡，于是连着调了两轮颜色和宽度，**调的全是不存在的东西**。
是断言 厚⑧（`mergeGeometries` 必须出现在源码里）把它抓出来的。

**② 塔基同样一次都没渲染过，我却对着截图说"地基出来了"。**
`towerFoundation` 这个配置块被我插进了 `Config.js` 的**顶层**而不是 `ui` 下面，
而消费方读的是 `CONFIG.ui.towerFoundation` → `undefined` → `foundation = null`。
画面上那个"更宽的台"其实是塔身本来就有的深色底阶。是新写的
`sim_towerbase.mjs` 起手第一条（`CONFIG.ui.towerFoundation` 必须存在）抓出来的。

**共同的根因：我在确认代码路径真的执行之前就对着画面下结论。**
下次改渲染，顺序应该是"先证明它在画面上存在（把参数调夸张到不可能看不见），
再谈好不好看"——这条本仓库已经在接地暗斑那次用对过一次，这两轮又忘了。

### 13.2 岸坡：竖直墙 → 外倾带

`TerrainEdgeLayer` 的几何从"沿轮廓摆 ~170 个竖直 Box"改成"沿轮廓挤出的连续外倾带"。

**为什么必须是斜面**：45° 正交下，竖直面**只有朝相机的那两条边看得见**，
背向的两条被陆地自己挡住，做多高都看不到。用户原话「桥下面的高度差是可以被看到的，
上面的看不到」说的正是这件事，跟崖壁高度无关。斜面有朝上的法线分量，任何方位角都吃得到光。

> ⚠️ 但别把话说满。外部评审提醒得对：单靠法线不等于"读得出高差"。
> 真正起作用的是**斜面 + 深度对比 + 轮廓 + 水面对比**四件一起。这一层只负责前两件。

**踩到的坑：外法线方向插反了。** 旧的竖直崖壁用 `(tz, -tx)` 当外法线，实测在真实
环上**50/58 个顶点指向陆地内侧**——竖直方块跨在边界上，插反了反而看不太出来；
换成挤出的斜面之后整条带子被陆地盖住。正确的是 `(-tz, tx)`。改完 56/58 朝外
（剩 2 个在凹角，是探针跨过去了，不是方向错）。断言 厚⑧b 守着这一条。

**抖动按块共用**，不是逐段独立随机：逐段随机读成噪声，按块共用才读成"大块段落"。

**颜色取地面色压暗一档，不能取墙的石色。** 第一版用了 `#6f89a6`（介于 `rockColor`
与 `wallCapColor` 之间），画面上岸坡读成"更多的墙"——正是这一轮要消灭的建筑感。
现在是 `#a8c2d4`（桥面 `#dce9f2` 压暗约 25%），读成"这块地的背光面"。

### 13.3 落差：为什么不采纳"水面抬到 −12"

外部评审建议 `waterY` 取 −10~−14。**这个方向是反的**，理由是屏幕空间的：

45° 正交下屏幕高差 = `|waterY| · sin45° · 视图缩放`。全图视角实测约
**0.344 像素/世界单位**（地图 2325 单位铺 800 像素）：

| waterY | 全图视角屏幕高差 |
|---|---|
| −24（v55） | 5.9 px |
| **−22（现在）** | **5.4 px** |
| −12（评审建议） | 2.9 px |

用户的诉求是"能看出来桥在水面上"，砍到 2.9 像素就彻底读不出来了。
评审把"墙太高（18、竖直、深色）"和"水太深"混成了一件事——墙的视觉权重过大是真的，
但那要靠把竖直面换成受光斜面来解决。断言 厚② 钉的就是"别再往回调"。

**`slopeRun` 取 8~10 而不是 14**：评审担心外扩会吃掉桥两侧的水面留白。
量下来这条担心在这张图上不成立（桥中点每侧有约 1400 单位的开阔水面，外扩 10
只吃掉 0.7%），但外扩确实会让**桥的视觉宽度**变大（桥半宽约 200，两侧各加 10 = +10%），
所以还是取了偏小的一档。

### 13.4 桥墙：降权重 + 打散节奏

评审说"似乎把同一套 wall system 同时拿去做陆地外缘和桥侧边"——**技术前提是错的**，
桥侧的柱+墙在 `HowlingAbyssDecor`，与 `TerrainEdgeLayer` 从来是两套代码。
但它的观感结论成立：这道墙在画面里几乎在跟桥面抢视觉中心。

它最好的一条是指出了**等距重复**：柱距是死板的 160（`GAP_D`），每根还各挂一盏灯，
整道墙于是读成"人工护栏 + 路灯阵列"。

柱子的位置是地图的**结构数据**（标的是桥原本变窄的地方），有断言 桥⑥ 钉着不能挪，
所以改动落在三处：
* 墙板 13→7、厚 6→8.4（矮而厚，读成路肩不是栏杆）；柱高 22→12。
* **火炬不再每根柱子一盏**：柱子带 `torch` 标记（每 `TORCH_PILLAR_STRIDE`=2 根一盏，
  两端必挂）。这个标记是唯一真相——`map.torches`（灯的位置）和渲染层的火盆几何
  都读它，两边不可能对不上。**副作用要知道：夜间桥上的光源数从 26 降到 14。**
* 柱高按弧长派生一个确定性系数做**块状起伏**，等高等距的节拍器感被打散。

### 13.5 塔基：为什么不能是"地面色压暗一档"

评审这条说得对，我采纳了：冰封图地面本来就亮（`#dce9f2`），压暗一档得到的仍是
一块深色贴片，等于把问题换个位置。正确的是取**地面色与塔身石色之间的中间色**。

**但真正让它失败的是几何不是颜色。** 第一版 `height 0.22 / spread 1.34` 做出来
采样是 (42,43,58)，**比塔身侧面 (93,66,38) 还暗**——因为高而窄的台座在 45° 下
只露侧面，而侧面吃不到光。改成**压扁加宽**（0.16 / 1.52）露出受光的顶面之后，
实测塔基 (160,132,121) 稳稳落在地面 (181,150,128) 与塔身 (45,32,14) 之间。

另外两条：
* 几何加在 **y<0** 一侧。`pack()` 会把模型底面强制对齐回 y=0，于是塔基自然落地、
  塔身整体上抬一个塔基高度，**不用去改塔身里每一处写死的 y**（也就不会漏改某一档）。
* 形状必须跟阵营的塔身走。第一版一律用 `F.crownSides` 拉圆柱，蓝方就出现了
  **方塔配六边形台座**，正是用户反复强调的"设计语言不统一"。

### 13.6 这两轮明确没做的

* 统一 `WORLD_PALETTE`（岸坡/桥/塔基目前各自从调色板取值，还没收成一套派生关系）。
* flat shading 统一。
* 真 3D 地形——评审也同意现在不做，理由与 §8.2.3 一致。
* 重做塔的造型。评审这条说得对：底盘没做好之前重做塔，只会得到**一座更漂亮的孤岛**。
* §11.5 的地图边界矩形色阶，仍未解决。
