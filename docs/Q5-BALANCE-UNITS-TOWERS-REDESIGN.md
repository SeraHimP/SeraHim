# Q5：平衡修复 + 新兵种/塔武器方案

本文档记录设计决策，防止长对话中途丢失。**2026-09-19：用户下达"继续修bug和做新东西，
一批做完"，本文档转入实现阶段**——除第四节（羁绊系统，明确推迟）和 5.2（参考方向，
还没定要不要做）外，其余各节均按下方数值开始实现，实现进度见每节末尾追记。

---

## 一、数值修复类（已定方案，待实现）

### 1.1 枢纽塔太弱（诊断完成，方案已定）

**症状**：用户反馈枢纽双塔（hq_tower）/水晶塔（base，都装闪电杖）伤害弱。

**根因排查**：不是 `CONFIG.tuning.adaptiveDamage.apMagicDamagePct=60%` 那条全局自适应折扣——
闪电杖是 `specialAttack` 武器，`CombatSystem.performAttack` 第613行命中就直接 `return`，
完全跳过普通攻击管线，伤害只来自闪电杖自己的 `_doTick`：`20%×AP×充能倍率`，每秒固定4跳、
和攻速脱钩。同样的原始数值，闪电杖的产出量级只有假想同数值物理塔（AD×攻速4.0）的两三成，
这才是偏弱的根因。

**方案**：`CONFIG.tuning.weapons.lightningApConvertPct`（AD→AP转化率，当前100%即1:1）调到
**200%**。已软编码，改一个数字即可，塔的初始AP和AP成长（`towerGrowth.apRatioOfAd`）同步放大。

### 1.2 风魂重做（已定稿："疾风连击"）

**问题**：现状是"小兵脱战+移速/塔+攻速"两截。用户实测指出更本质的问题——龙魂只发给
塔+大型小兵（`DragonSystem.SOUL_REWARD_OK` 排除近战/远程），"全体小兵移速"这个描述本身
是假的：一条兵线里只有图腾/术士/炮车这些大型小兵会提速，近战/远程原地不动，一队兵被硬生生
拆成两拨，**反而比不拿风魂更容易被各个击破**。

**定稿方案**："疾风连击"——命中一次目标，获得一层疾风，`bonusAttackSpeedPct` 每层 +8%，
最多5层（封顶+40%），走 `stackPolicy:'stack'`（EffectRegistry 原生支持：自动+1层、刷新
duration，不需要自己额外维护计数器，参考 `EffectRegistry.js` 177-196行）；`duration` 设为
3秒，3秒内没有新的命中则整层清空。塔与大型小兵共用同一套机制，不再分"塔一半/小兵一半"。
这是 `bonusAttackSpeedPct`，会自动走 `attackSpeedRatio` 收益率（`calcAttackSpeedOf` 公式：
`effectiveBonusPct = positive×attackSpeedRatio + negative`），用户已确认这就是"走收益率"的
意思，不需要额外处理。

原有 `towerBonusAttackSpeedPct: 35`（常驻攻速）、`moveSpeedPct`/`moveSpeedOutPct`（移速）
两截全部删除。`CONFIG.gameRules.dragonSouls.wind` 改成 `{ perStackPct: 8, maxStacks: 5,
decaySec: 3 }`。

### 1.3 术士兵/图腾兵/攻城车削弱 + 收窄定位（已定方向，具体数值待定）

**发现的结构性问题**：术士兵主动技能"蓄能打击"自身法强永久无上限叠加
（`maxStacks:999`，每次+5，事实上不封顶）——这可能是"术士兵太强"被低估的一块，
一个能存活的术士兵会无限膨胀，需要加上限。

**收窄方向（见第三节，与新兵种方案一起定稿）**：
- 图腾兵收窄成**只做"光环治疗"**：主动技能改常驻光环持续回复，去掉减伤光环和额外护盾光环，
  自身固定护盾保留（"扛线"天然定位）
- 术士兵收窄成**只做"光环增伤"**：只留一条光环数值，穿透/法强/自身叠层全部删除
- 蚀骨兵：相对已经单一（近战AOE削双抗+主动中毒），只做数值上的小削弱，不改方向
- 攻城车：对塔伤害倍率700%→约550%，攻城疲惫每次攻击叠层13→16（更快自我降速）

### 1.4 超级兵早弱晚强（已定方案）

现状：超级兵数值是常量，不随游戏时间变化，"水晶陷落后立刻满状态出场"是提前终结比赛的漏洞。

**方案**：新增被动技能，在超级兵**生成那一刻**按当前游戏时间快照一个 `allStatsPct` 修正——
游戏时间≤0min 时基础数值打4折，线性爬升到15分钟满状态（100%），15分钟后继续缓慢增长
（每分钟再多约1.5%，上限150%封顶）。倍率在出生那一刻定死，不随单位存活时间变化。

---

## 二、新发现的bug（待排查，先记录）

1. **闪电杖对带"伤害格挡"（damageBlock）的目标格挡失效**——闪电杖伤害走
   `performAttackDirect` 直接传入已算好的数值，`CombatSystem.js` 1573行那处格挡扣减逻辑
   可能没覆盖这条路径，需要对着代码确认根因。

2. **天气：雪盖视觉阈值突变**——积雪到某个阈值后地面直接变白，很突兀，应该是缓慢过渡到白色，
   不是硬切换。（`GroundTraceLayer.js` 的雪盖 alphaMap 渲染，或 `GroundTraceSystem.js` 的
   `snowGlobalTarget`/局部雪深插值逻辑，需要排查是不是哪里用了阶跃而不是渐变）

3. **兵踩出的轨迹机制用户没看懂**——雪盖的"单位经过局部踩低、留下比周围雪盖更浅的小径"这个
   效果，用户反馈目前没看明白/没看到，需要排查是真的没生效还是视觉上不明显。

4. **水洼状态常驻bug**——下雨时单位身上常驻着"水洼"减速状态，这是错的：应该只有单位【站在】
   水洼区域内时才有这个状态和移速降低，离开水洼就该消失。需要排查 `GroundTraceSystem.js`
   `_applyEffects()` 里水洼判定逻辑，可能是位置判定漏了或者判反了。

---

## 三、新兵种方案（2026-09-19：3.1~3.4 全部已实现）

设计前提（用户反复强调）：单位不做决策，只会机械攻击/触发——任何新机制都必须能落在
"命中触发/周期触发/阈值触发"这三种既有模式里，不能要求"选择目标/评估局势"这类决策。

### 3.1 重装车 / 龟甲车（命名待定，倾向"龟甲车"）

**定位**：塔攻击优先级最高的坦克。略高血量、超高双抗、自带伤害格挡；攻速极低、攻击力一般
（对塔有额外伤害，但要比攻城车温和得多——只是坦着打的副产物，不能抢攻城车的戏）。
主动技能：一段时间内获得伤害减免。

**待实现要点**：需要在 `CombatSystem.selectTarget` 里给这个类型加最高优先级权重，
与3.3工程兵的"塔保护工程兵"是同一套目标优先级逻辑，两者要放一起设计优先级顺序（互不冲突）。

命名备选：龟甲车（慢、硬壳、挨打为生，形象最直接）／铁壁兵／岩卫兵。

**实现记录**：类型名定为 `heavy`（"重装车"，命名沿用最初的临时名，没有改成龟甲车）。
优先级在 `CombatSystem.selectTarget` 的 `getPriority` 加了 tier 6（仅次于 3.3 工程兵保护
的 tier 7）。被动 `passive_heavy_vs_tower`（对塔额外伤害）+ 主动 `active_heavy_bulwark`
（法力攒满限时减伤）。测试：`tests/sim_heavy.mjs`。

### 3.2 治疗兵

**定位**：无攻击敌方能力，按攻速节奏对友军造成治疗（不是主动技能触发，是类似"攻击"的
固定节奏脉冲），每次治疗损失固定生命值+百分比当前生命值（自我消耗换奶）；脱战后拥有
高额生命恢复（换血的恢复窗口）。

**架构要求（用户明确提出，不能走捷径）**：用户后续还计划做一座"没有攻击能力、专职给友军
回血的塔"，所以"单位的攻击目标指向友方而不是敌方"必须做成底层架构的一个"目标阵营"开关，
不是治疗兵一个人的特例代码——现在全局都是写死"只认敌方"（`FactionSystem.enemyUnitsInRadius`
这类）。治疗兵和以后那座奶塔要共用同一套东西，包括专属的"治疗弹道"可视化（攻击特效目前
按武器/技能各自定义子弹外观，加一个治疗方向的弹道素材配上这个开关即可，不用另搭一套）。

**实现记录（2026-09-19，架构深度已跟用户对齐过）**：类型名 `healer`（"治疗兵"）。跟用户
确认过实现深度——不深改现有"索敌+追击+锁定+弹道飞行"这条敌方专用管线（工作量大、
牵连近战/远程兵战斗手感），改走独立通道：新增 `FactionSystem.alliesInRadius`（与
`enemyUnitsInRadius` 完全对称，这就是"目标阵营"开关本体，未来奶塔直接复用）+ 被动
`passive_healer_mend`（按自身攻速节奏找最近友军做单体治疗，自损固定值+百分比当前生命，
命中已满血目标不扣自身血；脱战时用 aura 效果叠加额外 healthRegen）。**唯一未完成的部分**：
"专属治疗弹道可视化"——机制本身走的是被动 `onFrame` 节流触发，不经过
`CombatSystem.performAttack` 的弹道管线，目前只是逻辑生效，没有配套的专属弹道渲染，
后续如果要做需要单独设计一条"周期治疗事件→渲染层订阅画弹道"的路子。测试：
`tests/sim_healer.mjs`。

### 3.3 工程兵

**定位**：无攻击能力，修复己方防御塔生命值；且能以33%效率**突破"加固城防"的节点封顶**
去修复超出节点的部分；己方防御塔被攻击工程兵的单位命中后立即切目标优先攻击那个单位
（保护工程兵）。

**已确认的技术路径**：全部塔默认自带"加固城防"节点封顶（`towerPassives.js`
`_fortifyRecalc`，节点数组如水晶塔33/67/100%——当前血量卡在哪个区间，恢复就只能封顶到那个
区间上界，越不过去）。现在所有治疗来源统一走 `applyHeal(entity, amount, healPower, maxHP,
capHP)`，`capHP` 传的就是 `entity._regenCapHP`。工程兵要做的是**调用治疗时不传这个cap**，
超出节点部分单独按33%效率结算——不用碰治疗管线本身，只是工程兵自己的调用方式跟别的
治疗源不一样。

"塔保护工程兵"与3.1重装车共用同一套目标优先级扩展，需要一起设计顺序。

**实现记录**：类型名 `engineer`（"工程兵"）。移动/修复逻辑走独立方法
`LaneMovementSystem._updateEngineer`（不推线，不进标准索敌/追击/攻击那条链路，机械选
"半径内最近的受损己方塔"，没有就原地待命）。塔保护逻辑是 `getPriority` 的 tier 7（比
3.1 重装车的 tier 6 更高——"救命"压过"引火力"）。节点突破直接调用 `applyHeal` 时对
封顶内的部分正常传 `capHP`、封顶外的部分单独按33%效率不传 `capHP` 结算，没有改
`applyHeal` 本身。测试：`tests/sim_engineer.mjs`。

### 3.4 唤灵兵（用户最喜欢这个，已有较完整方案）

**定位**：唯一一个"凭空多一个打架的身体"而不是"加成数值"的兵种。

**机制**：主动技能走现有法力槽架构（跟图腾/术士的主动技能同一套），法力攒满时在自己脚下
召唤一只幻灵——一个真正的小型战斗单位（复用现有小兵创建管线，不是特效摆设），存在
12~15秒后自动消失（或提前被打死）。唤灵兵自己血/攻偏低，指望幻灵抗线。

**幻灵具体设计**：
- 血量/攻击力取偏低量级（近战兵的60~70%），不成长，纯粹"临时补人头"
- 同时最多存在2只（软编码上限，防止无限叠加）
- 幻灵不计入屠戮/龙魂奖励/出兵编排统计口径（打 `_isSummoned` 标记排除）
- 视觉：半透明+灵体粒子，一眼看出是召唤物

**平衡杠杆（软编码）**：法力槽大小（召唤间隔）、幻灵存活时长、幻灵强度、同时存在上限。

**实现记录**：类型名 `summoner`（"唤灵兵"）。幻灵**没有新建兵种类型**——直接是
`type:'melee'` 实体，用 `combat.createMinion(type, x, y, faction, hpScale, attrScale)`
（新增到 CombatSystem 的后置注入，同 `setProjectileSystem`/`LaneWaveSystem.setCreateMinion`
一个模式）缩放出来，打 `_isSummoned`/`_summonExpireAt`/`_summonVisual` 三个标记。到期
由 `CombatSystem.update` 最前面一段统一判定阵亡（不走伤害管线）。`_isSummoned` 目前只在
`EntropySystem.onDeath` 里挡了"幻灵死亡不给对面记熵"这一条——用户原话"不计入
屠戮/龙魂奖励/出兵编排统计口径"里的"出兵编排统计口径"因为幻灵从不经过
`LaneWaveSystem`/`buildWaveOrder` 那条出兵管线，天然满足，没有额外代码；"屠戮/龙魂奖励"
两项没有再单独排查（幻灵是 `melee`，龙魂发放范围本来就不含 melee/ranged，屠戮命中幻灵
按现有屠戮规则正常结算——如果要连屠戮的"打幻灵不触发"也做，需要再补一条排除，这次
没做，如实记录）。**视觉上的"半透明+灵体粒子"没有实现**——`_summonVisual` 标记打好了，
但幻灵复用的是 `type:'melee'` 的 InstancedMesh 批量渲染（v51 那轮做的合批渲染），要给
批次里某一个实例单独调不透明度需要走实例属性而不是简单的材质 opacity，属于渲染层的
独立工作量，这次跟"目标阵营架构"的决策方式一样，选择先把游戏机制做扎实、可视化留到
下一轮单独做，不在这里囫囵实现一个可能有问题的版本。测试：`tests/sim_summoner.mjs`。

---

## 四、云顶之弈式"羁绊"系统——已明确推迟，这轮不做

用户认可这个方向（"这不是自走棋吗"），但明确说"羁绊先记下来，这轮不做"。

**思路记录**：给每个兵种打1-2个流派标签，战场上同时存活的某标签兵种数量达到阈值
（比如2/4档）时，全体带该标签的单位一起获得一档加成——云顶"集齐N个羁绊解锁效果"的思路。
比新增单个兵种的工作量大得多（要给所有兵种打标签、实时数数、分档加成），下一轮再单独立项。

---

## 五、塔的多样化方案（初版4个，用户要求"再想想"，见后续对话补充）

现状：塔只有穿透（物理双穿+升温，稳定输出）和闪电杖（法术DoT+减益，稳定输出）两条路，
手感高度同质。初版4个新方向：

1. **寒冰锥**——攻击范围减速+破甲，塔第一次有AOE控场武器
2. **汲取**——伤害按比例转化成自身护盾，塔第一次有"打人回血"的生存型武器
3. **连弩**——攻击有概率触发一次额外瞬间攻击，爆发不稳定输出，跟另外两条"稳定DPS"手感不同
4. **驱散者**——攻击附带清除目标一个正面状态，第一次有"反制/去buff"而不是纯造成伤害的武器
5. **锁链塔**——命中同一目标叠加减速，层数越高越接近定身，离开射程才清空，单体控场
6. **献祭塔**——周期性消耗自身当前生命值换一次范围爆发伤害，"拿自己血换输出"的风险/收益武器
7. **坠星塔**——延迟抛物线弹道，落地才结算范围伤害，跟"命中即时结算"的其它武器手感不同
8. **反震塔**——被攻击时概率反弹部分伤害给攻击者，唯一一个"靠挨打触发"而不是"靠自己出手触发"的武器

### 5.1 三个已细化到具体机制的塔武器（用户给了确切数值/规则，待定名+定稿）

**聚能塔（临时命名）**——充能慢、蓄力后打出超高范围伤害（"憋大招"节奏），跟坠星塔
（每次都有延迟抛射）不是一回事：这个是低频、单次巨额AOE。

**牧灵塔（临时命名）**——塔本身没有攻击能力，召唤一个召唤物出去打，塔专职给召唤物回血。
已定规则：
- 召唤物**拴在塔周围一定范围内**（不是沿兵线走位的独立小兵，是塔的随行守卫，方便塔持续够得着回血）
- 召唤物死亡后**隔一段时间重新召唤**，且**每次重新召唤的冷却时长递增**（每死一次，下次复活更慢——
  长期拖下去会越打越难维持，鼓励优先保护这个召唤物而不是放任它反复送）
- 召唤物**获得塔的一部分属性（按百分比）**——塔自己变强（成长/龙魂），召唤物也跟着变强
- 待定：召唤物的具体属性/血量量级、拴绳半径、初始冷却与递增步长——这些等其它方案定完一起定数值

**狂潮塔（临时命名，原"机枪塔"，还需要更好的名字）**——数值已理清，三个机制全部复用现有
既有技术：
- 攻击只造成55%伤害，攻击特效（on-hit类效果）按33%效率结算——复用闪电杖已有的
  `attackShare` 参数
- 总攻速+100%，**不走收益率**——加在 `baseAttackSpeed` 上而不是 `bonusAttackSpeedPct`
  （现有公式只有 `bonusAttackSpeedPct` 的正值部分才被 `attackSpeedRatio` 打折，
  `baseAttackSpeed` 从不打折，攻城车"普通模式+33%攻速"就是这么做的，直接复用同一思路）
- 攻击时每秒叠一层攻速加成，**走收益率**（`bonusAttackSpeedPct`）。**2026-09-19 定稿**：
  每层 +2%，最多15层（封顶+30%），**不按时间衰减**——用户明确要求"和风魂区别开来"，
  风魂是"命中叠层、限时衰减"的羊刀节奏，这条改成"脱战/切换目标才清空"：只要持续咬着
  同一个目标打就不掉层，层数缓慢堆到封顶，换目标或进入脱战状态立即清零重来。

### 5.2 参考其它塔防游戏再想的4个（Bloons TD / Kingdom Rush / PvZ 等，讨论中）

1. **诅咒塔**——不直接造成伤害，给目标打上"易伤"标记，标记期间目标受到的【全部】伤害
   （包括其它塔/兵的）都放大一定比例——第一个"纯增幅其它输出"而不是自己输出的塔，
   参考 Kingdom Rush 的奥术法师诅咒机制
2. **陷阱塔**——周期性在射程内的地面布一个静止的地雷，第一个踩上去的敌人触发爆炸——
   跟"瘴气兵"（兵走位主动拖痕迹）不是一回事，这个是塔在原地埋伏，参考 PvZ 土豆雷/
   Kingdom Rush 的炸弹陷阱
3. **狙击塔**——超远射程、极慢攻速、单发巨额伤害（高穿透/高破防），"稀有但致命的一击"，
   跟现有所有"持续输出"的塔完全是另一种节奏，参考多数TD游戏都有的狙击手塔
4. **多重目标塔**——同一时间锁定2~3个独立目标各打一下（不是范围爆炸，是分别瞄准多个人），
   跟寒冰锥/坠星塔那种"一次性炸一片"不同，是"雨露均沾式"的清杂兵武器

（这四个是否要做、做几个，还没定。）

### 5.3 光棱塔——连锁伤害，需要与雷魂区分（用户最后加的一个，之后不再加新的了）

用户要求"类似光棱塔"、伤害连锁，但要跟已有的雷魂（`dragonsoul_thunder`：命中后依次弹射
最多6个敌人、固定真实伤害、总伤害均摊）做出区分，不能是同一个东西换个名字。

**区分方案**：雷魂是"一条光沿目标依次弹射"（时序上一个接一个、固定真实伤害、均摊总量）；
光棱塔改成**"塔本身同一时刻分裂出多条独立光束"**（不是依次弹射，是从塔的位置同时向射程内
多个不同目标发射N条分支光束），伤害类型走物理/魔法自适应（不是雷魂的固定真实伤害），
命中目标数越多每条伤害有轻微递减（防止和雷魂一样无限乘算失控）。"光棱"（棱镜分光）这个
名字本身就对应"从源头一次性分裂"而不是"沿途弹射"，命名和机制是一致的。

用户明确表示"剩下的先不加了"——这是这轮塔武器构思的最后一个。
