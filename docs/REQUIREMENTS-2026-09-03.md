# 需求汇总 —— 2026-09-03 夜谈

用户当晚陆续提出、讨论、拍板的一批需求，趁着用户睡下先落成文档存底，
避免只留在对话记录里。**执行顺序按"改动小/已拍板 → 改动大/待细化"排**，
每做完一块单独测试+提交，不是攒一个大改动最后一起交。

---

## 一、地图编辑器：暂停 + 自由移动（已拍板，风险低，优先做）

1. **编辑时游戏不运行**：进入地图编辑器（不管是弹窗 `MapEditorDialog.js`
   还是主画面工具条 `MapEditorBoardTool.js`）就 `CTX.gamePaused = true`，
   记住进入前的暂停状态，退出时恢复成进入前的状态（不是无脑续玩）。
2. **架构定稿：方案 A**——同一个 `index.html`，不做第二个页面。
   第二个 index.html 需要先给 `CONFIG.customMaps` 接持久化（现在纯内存，
   刷新即丢），且会让"直接在当前战斗上现场改地形"这个能力失去意义
   （跳转页面=丢失当前对局的实时状态）。方案 A 没有这些代价。
3. **两个编辑入口要不要合并成一个**：**用户已追加拍板——不应该分开**。
   2D 缩略图（弹窗 `MapEditorDialog.js` 那套）应该是对"直接在画布上编辑"
   （`MapEditorBoardTool.js`）的**补充**，不是并列的第二套系统。具体形态
   待第二节展开实现时定（大方向：以画布编辑为主，缩略图承担"全局视角
   + 快速跳转/框选"的辅助角色）。此前"先不合并"的保守处理作废，
   合并方案在做第二节时一并设计。
4. **移动塔改成自由移动**：`MapEditorBoardTool._dragTo()` 和
   `MapEditorDialog.dragBuildingTo()` 都在调 `snapBuildingPos()`——
   只要塔有 `laneId` 就被强制吸附到那条路的折线上。改成只做世界边界
   夹紧（跟水晶枢纽这类没有 `laneId` 的建筑现在的"自由摆放"一致），
   摆错位置交给已有的结构校验红线去提示，不再由拖拽本身强制拦。

## 二、弹窗编辑器：窗口放大 + 加塔/编塔（已拍板）

1. **窗口放大**：`.modal-box` 全局上限是 `max-width:620px`，
   `MapEditorDialog.js` 的画布尺寸 `CANVAS_DISPLAY_PX=380` 就是为了
   塞进这个上限选的。给地图编辑器单独开一档更大的弹窗尺寸样式。
2. **加塔**："建筑摆放"模式现在只能拖已有的 `draftBuildings`，没有
   "点空地新增一座塔"——复用主画面工具条 `_addTowerAt` 那套"就近吸附到
   最近的路"逻辑。
3. **编塔属性**：现在选中一座草稿塔只能改档位（`STRUCT_TIERS` 下拉），
   改不了武器/技能/召唤水晶这些——复用现有的 `AttributeEditor`
   （选中草稿塔时弹出编辑面板），不新造一套编辑 UI。
4. **两个入口合并（用户追加拍板）**：2D 缩略图是画布编辑的补充，不是
   独立系统——具体形态：以 `MapEditorBoardTool.js` 的画布编辑为主视图，
   `MapEditorDialog.js` 那套缩略图/弹窗形态收编成主视图旁边的一块
   辅助面板（全局视角+跳转），而不是各自独立打开、独立暂停恢复的两个
   入口。
5. **新建塔的塔选择器（用户追加拍板）**：缩略图/画布旁新增一个"塔选择器"
   面板，覆盖三种新增方式：
   - 从零新建（走默认属性起步，跟现在 `_addTowerAt` 的默认值逻辑一致）；
   - 套用已有某座塔的属性（"格式刷"——选中一座参照塔，新塔直接复制它的
     武器/技能/属性，位置仍按点击处/就近吸附算）；
   - 存成模板、以后新增时直接套用模板（模板是"属性集合"的具名存档，
     不与某一座具体的塔绑定，可以反复套用到多座新塔上）。
   这一条把"加塔"从"造一座默认塔"扩成"造一座任意塔"，模板存储用什么
   载体（内存/localStorage）留到实现这一节时定，不在这里预先拍板。

## 三、界面规范新增第四条铁律（已落地，`docs/ui-standard.md` §四）

"所有 UI 中除了对技能/状态/兵种等的必要介绍外，删除所有告诉用户
这套机制怎么运行的废话。" 已经跑过一遍全 `src/ui/**/*.js` 的审查，
确认 **8 处违规，分布在 6 个文件**（`pagesWave.js`/`SettingsDialog.js`/
`open.js`×2/`pagesGameplayWorld.js`×2/`pagesSkillEffect.js`/
`MapEditorDialog.js`×2），每处都是"游戏机制说明该留 + 设计动机/内部实现
该删"的混合体，具体见对话记录里的表格。这批清理动作小、互相独立，
可以顺手做掉。

## 四、出兵编排 UI 完全重做 → 升级为"地形模板/玩法配置"引擎（范围已扩大，重新拍板）

用户原话"完全重做！！！"，痛点确认了三条（全选）：
- 缺少空间可视化——现在纯文字标签页选路，看不出地图/兵线的样子。
- 操作方式不直觉——出兵顺序靠数字输入配置，应该改成拖拽编排（像排一个
  出兵队列）。
- 信息结构混乱——全局规则/分路配置/波次节奏现在混在一个长页面里，
  应该拆成清楚的几块/几步。

**用户中途把范围扩大了一整档**："出兵编排 UI 应该算不上单纯的编排了，
应该算一个小型引擎。要求和地形编辑器兵线那里打通，编排可以独立选择
（和地图相独立），可能存在一张地图上，地图模板相同，但塔位/兵线/中立
生物/阵营可能都不同。并且最好做一个自动识别导入图片的模块，给一张标准
2D 地图就能直接识别导入。"——问了 4 个关键澄清问题，用户全部选了推荐项：

1. **拆分边界**：地形模板只留纯地形（世界尺寸/navgrid/走廊半宽/高低差/
   障碍物），塔位/兵线/中立生物/阵营/出兵规则全部下放到可独立选择的
   玩法配置层。
2. **编辑器归属**：新的"配置"编辑器合并进统一编辑器（跟地形编辑器同一个
   入口，是并列的一个模式），不做成独立的配置/编排编辑器。
3. **图片自动识别范围**：先只识别可行走地形（把现有手工跑 Python/PIL
   脚本的流程自动化），塔位/兵线/阵营识别留到以后。
4. **现有三张地图**：不迁移——召唤师峡谷/扭曲丛林/嚎哭深渊的源码保持
   现在"一个对象揉在一起"的写法不动，新架构只用于以后新建的地图；但
   提供只读提取，能拿现有地图的地形当模板去配新打法。

### 执行状态
- [x] 数据层地基：`src/data/mapComposition.js`（TERRAIN_FIELDS/CONFIG_FIELDS
      字段清单 + composeMap/extractTerrainFromMap/extractConfigFromMap）——
      `7cf8015`。MapSystem 等下游系统完全不用改，拆分只发生在编辑/存档
      时刻。17 条测试，三张真实地图逐字段验证"拆开再拼回去"零丢失。
- [x] 统一编辑器接入"配置"模式（Part A）——地形模板复用已有"起点地图"选择器
      （不重复造一份）；新增阵营增删面板（withFactionAdded/withFactionRemoved/
      pruneMapDataForRemovedFaction，删除阵营级联清理建筑与相关出兵流）；
      出兵开关（复用 LaneWaveSystem 已有的 map.spawnEnabled 覆写机制，只是
      给它一个可视化入口）；塔位/兵线跳转到已有的"建筑摆放"/"路径编辑"两个
      模式（这两个模式本来就是通用实现，不为阵营数变多而重做）。已知限制：
      3+ 阵营的单位描边色/小地图光点色仍会落到灰色占位（渲染层泛化不在这批
      范围内，属于"中立野怪泛化"独立后续项目）。138 条 mapEditorCore 测试
      +Playwright 实机验收（新增/删除阵营、出兵开关、跳转、保存全链路无误）。
- [ ] 出兵编排引擎本体的可视化重做（Part B，未做）：顶部地图缩略图画兵线，
      点击线选路/编辑，复用地图编辑器阶段六路径编辑那套 navgrid 渲染逻辑；
      出兵顺序改拖拽编排；且要把"出兵编排"从现在的全局单一规则
      （CONFIG.gameRules.laneWaveComposition）改造成真正按地图/配置独立——
      这需要新的按图覆写数据通路（现有 map.spawnEnabled 只覆写"开关"，不覆写
      "顺序/权重"），是这一项本体，不是画面重做完就自动有了。
- [x] 2D 地图图片自动识别导入（先做可行走地形识别，Part C）——见
      `src/data/imageImport.js`，已完成并接入统一编辑器地形模式。

所有新写的文案要遵守第三条的铁律，不写"为什么这么设计"的废话。

## 五、小兵寻路：从"精确路点"改成"前瞻插值点"（已拍板，方案 A）

`LaneMovementSystem._advanceAlongLane` 现在的转向目标是路点数组里的
**精确坐标**（`target = wps[idx]`），导致整波小兵在拐弯处会向同一个
像素点收敛，形成"漏斗"观感。改成 pure-pursuit 打法：转向目标从
"折线上前瞻一段弧长的插值点"算，拐弯天然被削成弧线。`_laneWaypointIndex`/
弧长这些下游一堆系统在用的记账逻辑（出兵间距、集火优先级、走廊排队力、
"外塔各在自己半区"校验）不用动，只改"往哪个方向使劲"这一步的算法。
方案 B（纯流场驱动，改动大风险高）明确不做。

## 六、多阵营架构 + 中立野怪泛化（最大的一块，需要先写设计报告）

### 需求原文
- 地图编辑器要能**增/减阵营**（不只是蓝红两个）。
- **阵营淘汰规则**：某阵营水晶枢纽被摧毁 → 该阵营停止出兵，且
  **攻击该阵营的路径也停止出兵**（不是整个敌对阵营停摆，只是"打向
  已淘汰阵营那条路"停）。
- 以后要支持**中立野怪阵营泛化**（巨龙只是其中一种）：出生地、
  出生路径、出生条件都要能在地图编辑器里配。
- 整体调性："丰富的玩法，乱七八糟打来打去，看起来爽"——多阵营混战，
  路径可能汇合。

### 三个已拍板的设计决定
1. **阵营淘汰后果**：塔/建筑保留，仍可以被攻击/反击（不是变中立、
   不是消失）。
2. **增/减阵营的时机**：地图作者时间——地图声明自己支持几个阵营、
   初始建筑/路径怎么配，对局中阵营数固定，不支持中途动态加入第三方。
3. **多阵营汇合路段的战斗**：自然交战，不需要额外规则——现有的
   `canTarget`/索敌半径已经是"附近任何敌对阵营都可以打"，汇合点自然
   变成混战区，不用另加机制。

### 已勘查到的现状（好消息 + 真正的卡点）
- `FactionSystem.js` 的阵营关系表**本来就是通用的**（`ENEMY_PAIRS` 是
  任意对集合，`declareEnemies()` 函数已经留好，一直没人调）——这层
  不用改。
- `MapSystem.js` 摧毁水晶枢纽时已经发 `map:mainNexusDestroyed` 事件
  （现在没人监听），`CTX.__towerRules.waveOn` 已经是按阵营独立的开关——
  "某阵营停止出兵"这半件事管道是现成的。
- **真正的卡点**：`LaneWaveSystem._enqueueForFaction` 第112行
  `const enemy = faction === FACTIONS.BLUE ? FACTIONS.RED : FACTIONS.BLUE;`——
  "这条路打谁"是代码里现算的"另外那一个"，不是地图数据里存的。多阵营/
  路径汇合要求路径数据本身能表达"这条路归属哪个阵营、打哪个阵营"，
  这是整个多阵营改造的地基。
- 全仓库搜了一遍，**27 个文件**在某种形式上写死了"正好两个阵营"
  （对称校验、配色表两值、基地圈两角点、出兵广播、龙魂加成分配……），
  分布在数据/系统/表现/UI 四层。规模上不小于这次做完的地图编辑器项目，
  加上中立野怪泛化只会更大。

### 执行方式
不直接改代码——先写一份设计报告（比照当初地图编辑器那份的规格）：
逐个过一遍这 27 个文件，分成"天生支持 N 阵营不用动"和"写死两阵营
要改成读数据"，给出阵营数据模型（地图声明支持哪些阵营、每条路归属/
目标阵营）、淘汰→停止出兵的完整链路、中立野怪泛化方案，最后给分阶段
实施顺序。这一块预计跨度最大，今晚未必能做完，前面五块做完之后再开始，
做多少算多少，每个阶段单独测试+提交。

**报告完成后用户已就 5 条待确认问题拍板**（详情见
`docs/REPORT-2026-09-03-multifaction.md` §9 与其后的对话记录）：
lane 数据模型选方案A / 龙魂击杀方独享 / 计分板每阵营一栏 / 秩序-混乱系统
在 3+ 阵营地图上禁用 / 删除阵营时级联全部删除相关数据。已按报告 §8 的
分阶段顺序开始实施，见下方执行状态。

---

## 执行状态

本文档创建时（用户已入睡，授权直接开始执行）：一到五节尚未开始实现，
第六节的设计报告尚未动笔。按上面顺序推进，每完成一节在这里勾一下。

- [x] 第一节（编辑器暂停+塔自由移动）—— `ba65731`
- [x] 第三节（第四条铁律 8 处文案清理）—— `47589ce`
- [x] 第五节（小兵寻路 pure-pursuit）—— `bdc5de3`。过程中一版实现引入了真实回归
      （sim_passthrough.mjs 未接敌穿越），已用 git-stash 基线对比确认、根因定位、
      修复并复测通过，细节见该提交信息。
- [~] 第二节：1/5（弹窗放大）已做——`5a20bdd`。加塔/编塔/两入口合并/塔选择器
      这 4 项留到入口合并一起设计，见该提交信息里的理由。
- [ ] 第四节（出兵编排 UI 完全重做）
- [x] 第六节：设计报告已写完——`docs/REPORT-2026-09-03-multifaction.md`。
      结论：27 个"写死两阵营"的文件里，真正需要专门设计的只有 5 处（龙魂结算/
      死亡计分板/秩序-混乱系统/塔朝向/lane 数据模型本身），其余是机械替换。
      给了 lane 数据模型的两个候选方案（推荐方案 A）、淘汰→停止出兵的完整链路
      设计、分阶段实施顺序，以及 5 条需要用户拍板的待确认问题。**报告写完即止，
      没有动一行代码**——按用户"先写报告"的原话，等确认后再排进下一批实现。
      用户已就 5 条问题拍板，开始按 §8 分阶段实施：
  - [x] 阶段1：地基数据模型 `map.factions`/`lane.spawns`（`FactionSystem.
        mapFactionsOf/laneSpawnsOf`，三张地图显式声明）—— `7157ece`。
  - [x] 阶段2：出兵链路——`MapSystem.isFactionEliminated`（现算不缓存）+
        `structureCensus` 泛化 + `LaneWaveSystem._enqueueSpawn` 淘汰门控 +
        `__towerRuleFor` 未声明阵营的 waveOn 兜底修正——`572d51d`。
        阵营淘汰后**真正停止出兵**这条用户最明确要求的行为已经跑通。
  - [x] 阶段3：编辑器校验放开——`buildingCountsSymmetric`/`attackTowerSpacingOk`/
        `crossFactionTowerSpacingOk` 按 `mapFactionsOf` 泛化，3+ 阵营地图不再被
        两阵营对称规则误判"不合规"——`dbbe2ff`。
  - [x] 阶段5：结算类专项——`dbbe2ff`。龙魂击杀方独享（`DragonSystem.js`
        factionKills/factionTotals/souls 惰性开第三阵营记账 + `equipExistingSoul`
        泛化门 + `CombatSystem.recordLastHit` 泛化）、计分板每阵营一栏
        （`main.js` 新 `scorerFactionOf`，归属改成"谁打的最后一击"而不是
        "死者阵营表里另一个"）、熵-阵营耦合在 3+ 阵营地图禁用
        （`WorldState._entropyCouplingApplies`）、塔朝向按 `lane.spawns`
        声明的目标阵营取（`towerFacing.js`）。过程中 DragonSystem 的表结构
        第一版做成完全惰性 `{}`，两阵营既有测试当场炸了 2 条（`ds.factionTotals.red`
        从恒为 0 变成 undefined）——诊断后改回预置 `{blue:...,red:...}`，
        只在写入路径泛化，详见该提交信息。
  - [ ] 阶段4：编辑器 UI 阵营增删入口（放在第二节入口合并之后做）。
  - [ ] 阶段6：中立野怪泛化——独立立项，本次不做（见报告 §7）。

  第六节到此**除阶段4（等入口合并）、阶段6（独立立项）外全部完成**，
  多阵营的地基数据模型、出兵/淘汰链路、编辑器校验、龙魂/计分板/熵系统/
  塔朝向五处结算类专项都已跑通并有测试钉住，两阵营地图全程逐位不变。
