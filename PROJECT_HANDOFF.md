# 项目交接说明书（写给接手的 GPT）

> 这份文件专门为"接下来这部分工作换 GPT 来做"这件事而写，跟 `CLAUDE.md`/
> `docs/DEVELOPMENT.md` 不是同一份东西、不是重复劳动：
> - `CLAUDE.md`（仓库根目录）是给**任何**AI协作者看的入口页，四条硬约束，先看这个。
> - `docs/DEVELOPMENT.md` 是完整的架构/坑位手册，`CLAUDE.md` 会指向它。
> - **这份文件**是"当前正在进行的具体任务"的交接记录——统治战场·水晶之痕
>   这张沙漠地图从"能跑但光秃秃"到现在的完整过程、每个文件的角色、
>   已经踩过的坑、用户目前给出的所有反馈原话、还剩什么没做。
>   目的是让你不用重新逆向工程一遍就能接着往下做，也不会把已经踩过的坑
>   再踩一遍。
>
> **读的顺序建议**：先读 `CLAUDE.md`（5 分钟），再读这份文件的"第一部分：
> 必须先知道的四条铁律"和"第二部分：当前任务全貌"，遇到具体系统再去
> `docs/DEVELOPMENT.md` 查对应章节。**不要跳过第一部分直接看代码**——
> 这个项目里好几个坑是"代码本身完全没问题，但不知道某条约定就会做出
> 用户明确否决过的东西"。

---

## 第一部分：必须先知道的四条铁律

这四条来自仓库根目录的 `CLAUDE.md`，是用户反复强调、违反了就算改动失败的
硬约束。这里逐条展开成"对你实际意味着什么"：

### 铁律 1：交付前 `node tests/run_all.mjs` 必须 0 失败

```bash
node tests/run_all.mjs      # 约 6~9 分钟，40+ 套测试
```

- 不接受"失败集合没变就算过"——本项目吃过亏（长期带着 6 个失败用例，后来查出
  5 个是断言自己写错了）。**基线是 0 失败**，没有例外。
- 你新增的任何行为都必须配一套 `tests/sim_*.mjs`，并把文件名加进
  `tests/run_all.mjs` 的 `suites` 数组，否则等于没做——没断言守着的行为，
  下一次改动就会被悄悄改没。
- 断言要钉**行为的形状**（比如"外圈颜色浅、内圈颜色深、单调变化"），
  不要钉**具体数字**（比如"外圈必须是 #ffffff"）——数值以后会调，形状不会变。
- 渲染层（`src/presentation/*.js`）的测试有个本仓库特有的技巧：**这些文件
  大多数可以在纯 Node 环境直接 `import`**（`vendor/three.module.js` 的
  Group/Mesh/Geometry 构造不依赖浏览器全局，只有真正建 WebGLRenderer/Canvas
  才需要），所以能用一个假 `scene = {children:[], add(o){...}, remove(o){...}}`
  真的跑 `build()`/`update()`，直接断言真实产出的网格数量/位置/颜色——
  比"读源码正则找方法名存在"强得多，参见 `tests/sim_dominionprops.mjs`
  的写法（下面第二部分会详细讲这份测试踩过的两个坑）。

### 铁律 2：一切数值都要软编码

用户原话：「所有的都不要硬编码，都应该是可编辑的软编码」。新的玩法数值一律进
`src/data/Config.js`。**但纯装饰性的美术常量（颜色/密度/尺寸这类不影响任何
仿真结果的值）本项目有明确先例允许留在模块内当命名常量**，不强制塞进
`CONFIG`——见 `HowlingAbyssDecor.js`、`DominionPropsLayer.js` 头注里对这条的
说明。判断标准：**改这个值会不会改变仿真结果（伤害/占领/出兵/胜负）**——
会，进 CONFIG；不会（纯长什么样），可以留在文件里，但要写注释说明这是刻意的。

### 铁律 3：改了期望常量必须在提交信息里逐项报告

哪个常量、从什么改成什么、为什么。没改也要写"本次未改动任何期望常量"。
提交信息用中文，把"为什么"写清楚，尤其是"原来的判断哪里错了"——诊断错了
要如实说，这是本仓库刻意保留的习惯，能让后来的人少走弯路。

### 铁律 4：涉及主观视觉判断的改动，写代码前必须先跟用户对齐

这条是**这次交接最重要的一条**，用户是在连续两轮森林风格返工后立的规矩，
原话：「你写代码之前必须和我确认，写进铁律，必须每次都遵守」。

**什么算"主观视觉判断"**：美术风格、几何形状、颜色、密度、布局这类
"没有唯一正确答案、只能靠具体描述或参考图收敛"的东西。

**什么不算**（可以直接写代码，不用每次确认）：需求已经给出明确、无歧义的
规格（具体数值、明确的逻辑分支、已有先例可以照抄）的实现工作。

**这次踩过的教训**（详见第二部分）：即使有用户认可的方案，"往上面撒装饰"
这种做法本身可能就是对问题的误判——用户转述的 GPT 评估原话："它把'不可
行走区域'做成了一个巨大的黑洞……完全没利用'沙漠峡谷'这个主题……没有
视觉层级"，"不是没干活，是干了最容易干、但对问题帮助最小的那种活"。
**教训是：视觉问题不一定是"细节不够"，有可能是"结构没搭对"，撒更多小
装饰不会把"平"变得"不平"。** 遇到"看起来空/丑/不对"这类反馈时，先问
"这是缺细节还是缺结构"，不要默认往上加东西。

---

## 第二部分：当前任务全貌——统治战场·水晶之痕 沙漠地图视觉改造

### 2.1 这个任务是什么

`dominion_crystal_scar_v1`（label：统治战场·水晶之痕）是仿《英雄联盟》已
下线的"统治战场"模式的一张环形地图：7 个可争夺据点沿圆环分布，双方各有一座
水晶枢纽（不在环上，通过一条走廊接入环），占领据点决定出兵和对方水晶掉血
速度。**核心机制（占领/出兵/胜负判定）已经在更早的会话里做完并且稳定**，
`tests/sim_dominion.mjs`（140 项断言）覆盖这部分，**这不是当前任务的范围，
不要动**。

当前任务范围是**纯视觉**：这张图机制上没问题，但用户实机截图后反馈"光秃秃
一片"，随后用 GPT 做了三轮视觉评估，每轮都指出上一轮改得不够/改错方向，
到目前为止已经做了三轮迭代。

### 2.2 用户反馈的完整演变（原话摘录，按时间顺序）

这部分很重要——理解"为什么现在长这样"比直接看代码更快，尤其是理解
"为什么不能只靠撒装饰解决"这件事，必须看完第三轮反馈的原话。

**第一轮**（起点）：用户看完据点占领机制的实机截图，反馈"这个地图光秃秃的，
我要让 GPT 帮我评估怎么优化地图"——这是本任务的起点，用户主动要用 GPT
做设计评估，不是我自己臆测方案。

**第一轮 GPT 方案**（用户认可后执行）：针对性做"实体化具体场景物"——风车
据点摆一座风车、钻机摆一台钻井机械、采石场堆矿坑、兽骨场散骨架、精炼厂放
油罐管道。这轮做完之后，**实机截图里这些道具虽然确实被建出来了（用
`page.evaluate` 数过场景里的网格数量，40 个子节点），但相对于地图的世界
尺寸和默认相机距离，个头太小、跟背景色对比不够，人眼完全看不出来**——这是
一个"逻辑上做了，视觉上等于没做"的典型案例，值得记住。

**用户原话（第二轮开场）**："哈哈，这个我可以直接开骂。这版最大的问题不是
'做得不好看'，而是 Claude 根本没理解你要解决的视觉问题。" 随后转述的完整
GPT 评估（**这段原话非常长，建议直接翻聊天记录看完整版，这里摘要最关键的
几条判断**）：

1. "它把'地形'理解成了'贴图装饰'……你原图最大的问题是'平、空、单调'，
   它的解决方案居然还是继续保持一个巨大的平面，然后往上撒装饰。"
2. "它把'不可行走区域'做成了一个巨大的黑洞……而不是'这里是一片不可通行的
   巨大峡谷/岩丘/荒漠地貌'。"
3. "它完全没利用'沙漠峡谷'这个主题……五个据点完全没有形成五个视觉地标。"
4. "它最大的问题：没有'视觉层级'……一张好的地图应该至少有：第一层大形
   （一眼看过去：沙漠峡谷/环形竞技场）→ 第二层中形（岩丘、沙丘、峡谷、
   废墟、矿坑、工业区）→ 第三层小形（石块、骨头、木桩、残骸）→ 第四层
   功能物（据点、基地、塔）。Claude 直接跳到了第三层+第四层。"
5. "更严重的是，它把你的环形地图做成了'跑道'……不是'我先画了一条路→然后
   在外面放石头'，而是'先有沙漠地形→地形中自然形成了一条可通行峡谷→兵线
   恰好沿着峡谷走'。"
6. "它还犯了一个特别典型的 AI 错误：'觉得空=加东西'……真正需要解决的是
   '大面积单一色块没有结构'。"
7. 关于颜色："全地图只有这一种地面颜色……正确方向应该是同色系分层：主沙地/
   浅沙丘/风蚀沙地/红褐裸岩/深褐岩壁/近黑峡谷阴影……不是增加彩虹颜色，而是
   同一个沙漠色系里建立 5~7 个明度/饱和度层级。"
8. GPT 给的最终判决（用户明确要求照这个方向做）：
   > "不要把'不可行走区域'继续当成一块深色背景；它必须视觉上成为沙漠峡谷、
   > 岩壁、裸岩、沙丘和地貌结构。不要通过大量随机小石头解决空旷问题……
   > 地图应该遵循：大尺度地形→中尺度主题区域→小型环境细节→功能建筑。
   > ……色彩不要增加大量新颜色，继续使用沙漠暖色系，但建立至少六级左右的
   > 视觉层次。整体仍然必须保持：Thronefall式低多边形、平面色块、硬切面、
   > 简洁轮廓、低纹理、非写实。……不要再用'增加小物件数量'解决视觉空旷
   > 问题。先解决地形结构。"

**这是第三轮实际执行时的直接指导文本，不是我自己概括的，做后续任何调整
时应该重新对照这段原话检查有没有跑偏。**

### 2.3 三轮迭代分别做了什么（文件层面）

#### 第一轮（更早会话完成，本次会话开始时已存在）

`src/presentation/DominionPropsLayer.js` 首次创建：5 个据点各一套主题道具
（`buildWindmill`/`buildDrill`/`buildQuarry`/`buildBoneyard`/`buildRefinery`），
低多边形几何 + `MeshLambertMaterial flatShading`，放在节点周围沿"背离环心"
方向推出去的位置（`RADIAL_OFFSET`）。**问题**：个头太小，看不见。

#### 第二轮（本次会话，针对"用户第一次反馈+确认要旗帜"）

用户明确确认了两点（`AskUserQuestion` 问的）：
1. 据点旁的彩旗小塔要不要做成新功能（旗色跟随占领状态实时变化）——
   **要做（用户选的推荐项）**。
2. 实现顺序——**用户选了"一次性全做"，不要求分阶段验收**。

做的事：
- 5 个据点道具整体放大 1.4~1.8 倍，每个补 1~2 件关联道具（读成"功能区"）。
- 新增 `buildFlag()`：据点旁一根旗杆+旗面，旗色跟随 `_captureOwner`
  （复用 `resourceBar.js` 的 `RESOURCE_COLORS.capture_blue/red/neutral`，
  跟头顶占领进度条同一套色，不是另起一套）。**覆盖全部 7 个据点**，包括
  两个还没定稿主题名字的"商栈"/"望塔"占位据点——不能因为名字没定稿就
  少一份反馈。
- 新增 `buildNexusFlank()`：水晶枢纽两侧各一根固定阵营色的旗杆装饰
  （不重做水晶本体，那是 `UnitLayer.js` 的塔模型职责）。
- 新增 `buildTerrainAccents()`：不可走区域撒岩石簇/枯木/骨渣，**只看
  `isWalkable`**，绝不摆在小兵能走的地方。

跑完这轮，实机截图**确实有肉眼可见的改善**（岩石/道具能看出来了），
但用户拿去问 GPT 得到了 2.2 节那段"根本没理解视觉问题"的批评。

#### 第三轮（本次会话，对着 GPT 评估重做）

这轮不是在第二轮基础上"再加东西"，是**结构性补课**，对应 2.2 节的判决
逐条落地：

**① "黑洞"变真峡谷** —— 复用 `TerrainEdgeLayer.js`（这是本仓库已有的
**通用件**，最早为嚎哭深渊冰封版做的："两方基地变成不规则的大陆，中间用
桥连接，大陆和桥要做出立体感"，机制是：地形的不可走区域被挖空（alpha=0），
在更低的 `waterY` 铺一张深渊面，沿可走边界的轮廓画一圈外倾斜坡——陆地因此
第一次有了真实的高度落差，不再是一整块平面）。

做法：
- `src/data/maps/dominion_crystal_scar.js` 的 `DOMINION_CONFIG` 新增
  `terrainEdge: { waterY: -22, slopeColor: '#976d44', abyssColor: '#201409' }`。
  颜色不是拍脑袋定的，是从调色板的 `corridorColor`/`groundColor` 按
  "同色系压暗"规则派生（跟下面的 `deriveDesertRamp()` 同一套规则）。
- **⚠️ 关键坑（几乎一定会踩，务必先读）**：`terrainEdge` 这个字段之前
  **只有不走 `composeMap()` 的手写地图**（`howling_abyss_frost.js`）用过。
  `dominion_crystal_scar.js` 走的是 `composeMap({terrain, config})` 拼装
  流程（见 `src/data/mapComposition.js`），这个函数**按白名单过滤字段**——
  `TERRAIN_FIELDS`/`CONFIG_FIELDS` 两个数组里没登记的字段会被**静默丢弃**，
  没有任何报错提示，画面上就是"什么也没发生"。第一次加 `terrainEdge`
  必须同时把它加进 `CONFIG_FIELDS`（已经加了，在 `mapComposition.js`
  搜 `'terrainEdge'` 能看到）。**以后往任何走 `composeMap()` 的地图加
  新的顶层字段，第一件事永远是检查这两个白名单数组里有没有它**——这是
  本仓库这几天连续踩过好几次的坑（`dominionNodes`、`waveEditorLaneIds`
  都在 `mapComposition.js` 的头注里留了同样的踩坑记录）。

**② 大尺度地貌，不是撒石头** —— 新增 `buildCanyonMonoliths()`：中央峡谷/
外圈放 6 座真正大的岩体（40~90 高，比原来的碎石簇高一个数量级），**沉到
`terrainEdge.waterY` 那个深度**（跟 `HowlingAbyssDecor.js` 水域装饰同一条
规则："陆地有厚度之后，水域装饰要整体沉到深渊面那一层"）。原来那批小岩石/
骨渣/枯木（`buildTerrainAccents`）密度从 0.46 调到 0.3，降级成巨岩周围的
"第三层细节"，不再是撑起空间感的主角。

**③ 沿途色阶** —— 新增 `buildSandTexture()`：沿**整条环形走廊**（不只是
据点周围）撒浅色沙丘高光块 + 深色风蚀阴影块，只画在**可走**区域（跟
`buildTerrainAccents`/`buildCanyonMonoliths` 的门控方向刚好相反——一个
只认可走、一个只认不可走，写测试时这是个坑，见 2.5 节）。

**④ 每个据点一块专属地面色** —— 新增 `buildNodeGroundPatch()`：5 个主题
据点的功能区脚下垫一块跟主题呼应的地面色（采石场偏灰岩、兽骨场偏骨白、
精炼厂/钻机偏油渍暗褐、风车维持亮沙色），让"据点+功能区"读成同一片场地。

**六档颜色的来源** —— 全部由 `deriveDesertRamp(SV)` 函数从调色板已有的
`corridorColor`/`groundColor`/`rockColor` 用 `THREE.Color.lerp`/
`multiplyScalar` 派生（同一色相家族深浅分层，不是新起一套配色），这是
用户转述的 GPT 方案明确要求的做法（"不要增加彩虹颜色……同一个沙漠色系
里建立 5~7 个明度/饱和度层级"）。**以后需要新的装饰颜色，先看
`deriveDesertRamp()` 里有没有能用的，没有就照着它的写法加一档，不要
另起一个十六进制字符串**。

### 2.4 涉及的全部文件清单（这次任务范围内）

| 文件 | 角色 | 这次改了什么 |
|---|---|---|
| `src/presentation/DominionPropsLayer.js` | 核心装饰层，本次工作量最大 | 见 2.3 节，全部三轮改动都在这个文件 |
| `src/presentation/ThreeRenderer.js` | 渲染主循环，装配各个 Layer | 新增 `dominionProps.setWalkableFn(...)` 注入 + 每帧 `dominionProps.update(this.deps)` 调用（旗帜刷色） |
| `src/data/maps/dominion_crystal_scar.js` | 地图数据（节点/navgrid/建筑/数值） | 新增 `terrainEdge` 声明；**占领机制/出兵/navgrid 部分本次没动** |
| `src/data/mapComposition.js` | `composeMap()` 白名单 | `CONFIG_FIELDS` 数组新增 `'terrainEdge'`（见 2.3 节①的坑） |
| `tests/sim_dominionprops.mjs` | 本次新增的测试文件 | 27 项断言，覆盖三轮全部新行为，见 2.5 节 |
| `tests/run_all.mjs` | 全量测试入口 | `suites` 数组里加了 `'sim_dominionprops.mjs'` |
| `tests/sim_navoutline.mjs` | 老测试，需要跟着更新 | 有一条断言原意是"只有嚎哭深渊冰封版声明了 `terrainEdge`"，dominion 这张图接入后这条断言的排除名单要跟着加——**忘了同步这一条会让全量回归红一个跟你的改动毫不相关的用例，容易误判成"改坏了别的东西"，其实只是需要把新的合法使用方加进排除名单** |
| `src/core/resourceBar.js` | 只读，没改 | 提供 `RESOURCE_COLORS.capture_blue/red/neutral`，旗帜刷色复用它 |
| `src/systems/DominionSystem.js` | 占领机制本体，**这次没碰** | `node.entity._captureOwner` 字段是旗帜读数据的来源 |
| `src/presentation/HowlingAbyssDecor.js` | 只读参照，没改 | 本项目"专属装饰层"的范式来源，`DominionPropsLayer.js` 的写法全部照抄它的约定 |
| `src/presentation/TerrainEdgeLayer.js` | 只读复用，没改 | `terrainEdge` 机制的实现本体，这次只是"声明使用"，没有改它的代码 |

### 2.5 `tests/sim_dominionprops.mjs` 详解（27 项断言，写测试时踩过的坑）

这份测试文件是这次任务里**唯一新增**的测试文件，覆盖三轮全部行为。写的
时候踩过两个值得记录的坑，都是"重构了产品代码，测试的计数方式没跟上"：

**坑 1：子分组导致顶层 `children.length` 统计失真**。第三轮把沿途色阶/
地形装饰挪进了具名子分组（`sandTexture`/`voidAccents`），只查
`layer.group.children.length` 数不到子分组里面真正的网格——修法是写一个
`countDescendants()` 递归计数器，需要精确核对某一类内容时用
`layer.group.children.find(c => c.name === '...')` 先找到那个具名子分组，
再在它上面计数，不要笼统数顶层。

**坑 2：两套系统的 `isWalkable` 门控方向相反，不能用同一个"整图对比"
证明"门控生效"**。`buildTerrainAccents`/`buildCanyonMonoliths` 只在
**不可走**的地方摆东西，`buildSandTexture` 只在**可走**的地方摆东西——
"整张图全部可走" vs "环形可走区" 这种笼统总数对比，两套系统此消彼长，
总数不一定沿哪个方向单调。**正确做法是"全图可走"vs"全图不可走"两个
极端场景分别核对每个具名子分组的计数**（`voidAllWalkable === 0 &&
voidNoneWalkable > 0`，`sandNoneWalkable === 0 && sandAllWalkable > 0`），
不要指望一条笼统断言能同时证明两件方向相反的事。

**坑 3（更早，第二轮踩的）**：`update()` 方法最初跨帧缓存了据点实体引用
（`entityRef`），当测试模拟"实体对象被替换成新对象"（比如归属从红方翻转
成中立，产生一个新的实体对象而不是原地修改）时，缓存的旧引用导致旗色卡在
上一个状态不刷新。修法：改成**不跨帧缓存**，每帧都重新查
`deps.entities.getAllTowers(false)`——这个查询本身是 O(塔数量) 的分桶
查询，一帧被其它系统调约 10 次（`EntityContainer.js` 的 `getAllMinions`
头注有同样的先例说明），这里再调一次量级完全一样，用一点点查表开销换
正确性是划算的。**如果你要给别的渲染层加"实时读取仿真状态"的功能，
默认不要跨帧缓存引用，除非有性能实测证明必须缓存。**

### 2.6 已经做完、经过验证的部分（可以放心当作既定事实）

- `terrainEdge` 机制在这张地图上跑通了（实机截图确认：不可走区域从纯色
  背景变成了深蓝色的深渊面，陆地边界能看出真实的高度落差）。
- 沿途色阶、5 个据点专属地面色、中央大岩体，实机截图都能看到明显效果
  （比第二轮的"光秃秃"截图有质的改善）。
- 归属旗跟随占领状态实时变色，逻辑测试完整覆盖（27 项断言），但**实机
  截图里因为默认相机缩放太远，旗子本身看不太清楚**——功能是对的，视觉
  呈现在近距离/放大视角下才看得出来，这属于"逻辑做对了，默认镜头下不
  显眼"的情况，跟第一轮"逻辑做了但完全看不见"不是一回事，但如果用户
  反馈"看不到旗子"，先怀疑是镜头距离问题，不要怀疑逻辑。
- `node tests/run_all.mjs` 全量测试跑绿（提交前的最后一次验证以本文件
  末尾"当前状态"一节记录的时间点为准）。

### 2.7 还没做、GPT 方案里提到但这次没实现的部分

这些是 2.2 节 GPT 评估里提到、但受限于工程复杂度/风险这次**刻意没做**的，
如果用户要求继续深入，从这里接着做：

1. **真正的高度场地形（沙丘的物理凸起、道路的真实起伏）**。这次做的是
   "平面色块 + 装饰性凸起物"（`buildSandTexture` 的团块贴地很扁，只有
   1.6 高，是色块不是地形），**不是**逐格高度场。真正做地形起伏需要碰
   `MapSystem.heightAt`/navgrid 的高度插值，`docs/DEVELOPMENT.md` §6.5
   "改地形要同时失效三样缓存"是这条路线要注意的坑。工程量和风险都明显
   更大（会影响小兵的视觉高度/阴影/镜头遮挡计算），这次评估后决定先用
   "挖空+深渊面"（`terrainEdge`）这条更轻量的路线制造高度错觉，效果已经
   解决了"黑洞"这个最刺眼的问题，是否还要做真高度场建议先让用户看现在
   的效果再决定要不要投入。
2. **五个据点的"功能区图样"更精细化**（GPT 方案里画的 ASCII 示意图，
   比如"风车+木桩+碎石+沙丘"围成一圈的具体布局关系、"精炼厂管道横竖向
   构图"）。现在的实现是"主题道具 + 关联小物件"松散摆放，**没有**照着
   GPT 那份"功能区示意图"逐一还原空间关系。这部分明确属于铁律 4 的
   "主观视觉判断"范畴，如果要继续做，应该先把每个据点的具体布局方案
   （类似 2.2 节 GPT 给的那种示意图）跟用户过一遍再动手，不要自己再猜
   一版摆法。
3. **天气/氛围联动**（沙尘天气效果、热浪扭曲）。GPT 原话里这条是"如果
   实现成本太高可以跳过"的可选项，这次没做。仓库里已有的 `WeatherSystem`/
   `PostFX.js` 体积雾管线理论上可以扩展出"沙尘"变体，但没有评估过具体
   工作量。
4. **两个水晶枢纽的水晶本体重做**（GPT 反馈"两个基地现在太像'普通塔'"）。
   这次只加了 `buildNexusFlank` 的两侧旗杆装饰，**没有**改水晶模型本身
   （那是 `UnitLayer.js` 的塔模型职责，不在 `DominionPropsLayer.js` 范围
   内，需要额外评估要不要跨文件改动）。
5. **据点占位名"商栈"/"望塔"还没有定稿的主题**——`NODE_GROUND_TINTS`/
   `BUILDERS` 只登记了 5 个正式主题（`windmill`/`drill`/`quarry`/
   `boneyard`/`refinery`），这两个据点目前只有归属旗、没有专属道具和
   地面色块。等用户给这两个据点定稿主题名字和内容方案后，照着
   `BUILDERS`/`NODE_GROUND_TINTS` 的既有模式各加一个条目即可，是个
   结构清晰的"填空"，不是难点。

### 2.8 如果用户反馈"还是不满意"，下一步怎么排查

按这个顺序检查，大概率能找到问题在哪层：

1. **先确认是不是相机/缩放问题**——本项目默认相机缩放较远，很多改动
   "逻辑做了但看不清"是这个原因（第一轮的教训）。用 Playwright 截图
   时默认视角下看不清的东西，实际游戏里玩家可以缩放，不代表没做对。
2. **再确认是不是"又在撒装饰"**——重新对照 2.2 节 GPT 判决的原话检查，
   尤其是"大尺度地形→中尺度主题区域→小型环境细节→功能建筑"这个顺序，
   如果发现自己又在往"小型环境细节"这一层加东西而没有先确认大尺度结构
   是否真的解决了，停下来重新看一遍这段话。
3. **确认改动是不是真的生效了**——参照 2.3 节①提到的 `composeMap()`
   白名单坑，任何新增的顶层地图字段第一件事检查 `TERRAIN_FIELDS`/
   `CONFIG_FIELDS`。
4. **用本文件 2.5 节的测试写法验证，不要只靠肉眼截图判断**——真实
   Three.js 几何测试能在几秒内确认"这个函数到底有没有被调用、产出了
   多少个网格、颜色对不对"，比反复截图-肉眼判断快得多。

---

## 第三部分：通用工程约定（摘自 `docs/DEVELOPMENT.md`，浓缩版）

完整版在 `docs/DEVELOPMENT.md`，这里只摘"这次任务范围内最容易踩、
以后大概率还会遇到"的几条：

### 3.1 系统间禁止互相 import

`src/systems/*.js` 之间不能互相 import，耦合走 `EventBus` 或
`main.js` 里单向监听。`src/presentation/*.js`（渲染层）**只读实体，
禁止往实体上写字段**——渲染层自己的状态放渲染层自己的对象/`WeakMap`。
`src/data/` 下的纯数据模块例外，谁都能 import（`waveComposition.js`/
`dragonCurve.js`/`towerModels.js`/`mapValidate.js` 都是这一类）。

### 3.2 同一个量只能有一个取值口

本仓库最贵的一条规则，反复出过事故（塔层级解析抄了四份、昼夜相位两处
各读一份……）。**发现自己在写"这个值应该怎么算"的第二份逻辑时，先搜
一遍这个计算是不是已经存在别处**，把它抽成一个函数放 `src/data/`，
两边都 import 它。

### 3.3 Three.js 渲染层的几个物理坑

- `PointLight.intensity` 是坎德拉不是照度，别直接填小数当亮度用。
- 光源数量必须恒定（改数量要重编译全部材质），塔灯是固定大小的池，
  不用的把 `intensity` 设 0，不要动态增减光源对象。
- **躺平的几何体（`rotateX(-Math.PI/2)`）要用 `DoubleSide` 材质**——
  旋转会翻转三角绕序，单面材质从上往下看正好是背面、整层被剔掉，
  查半天查不出来（instanceCount/属性/shader 全对，就是绕序翻了）。
- 子节点默认继承父节点的旋转/位移，想要子节点独立运动要用**相对**速度
  （子节点世界速度 = 父速度 + 子自身速度）。

### 3.4 地图数据的两个常见坑

- **`composeMap()` 白名单**——本文件 2.3 节①详细讲过，任何新顶层字段
  先检查 `TERRAIN_FIELDS`/`CONFIG_FIELDS`（`src/data/mapComposition.js`）。
- **基地圈的圆心是世界角点，不是水晶枢纽**——`baseCenters` 没声明时圆心
  默认是世界角点（蓝=左下、红=右上），已经坑过两次（按"离水晶枢纽多远"
  算出来的半径套在角点上会完全对不上）。这张沙漠环形图用的是自己的
  据点+水晶枢纽体系，不走 `baseCenters`/`baseCircleRadius` 这套（虽然
  `DOMINION_CONFIG` 里为了兼容某些下游读取声明了这两个字段，语义上
  它们在这张图上不是"传统三路地图的基地圈"，改动前确认清楚哪些代码
  真的在读它们）。

### 3.5 测试规范三条

1. **纯逻辑直接调产品代码，不要在测试里抄一份公式**——抄的那份永远会通过。
2. **断言钉行为形状，不钉具体数字**——数值以后会调，形状不会变。
3. **参数化不许偷偷改行为**——把写死的值搬进配置/常量时，默认值必须
   与原式可证明等价，用断言钉住。

---

## 第四部分：常用命令速查

```bash
# 全量回归（提交前必跑，0 失败才能提交）
node tests/run_all.mjs

# 只跑这次任务相关的测试（跑得快，开发过程中用这个）
node tests/sim_dominionprops.mjs
node tests/sim_dominion.mjs

# 起本地静态服务器看实机效果（本项目无构建步骤，纯静态文件）
python3 -m http.server 8811 --bind 127.0.0.1
# 然后用 Playwright（/opt/pw-browsers/chromium）截图，或者如果是人类用户
# 直接浏览器打开 http://127.0.0.1:8811/index.html

# 用 Playwright 截图看实机效果的固定套路（页面里没有 URL 参数直接选地图，
# 要走 UI 点击）：
#   1. document.getElementById('modeBtn').click()   打开"游戏地图"窗口
#   2. document.querySelector('[data-mode-id="dominion"]').click()  选统治战场模式
#   3. document.getElementById('modeOkBtn').click()  确定
# ⚠️ 这个项目在无 GPU 的沙箱环境里用 SwiftShader 软件渲染，帧率极低，
#    游戏内时间推进比真实时间慢约 7 倍（每 10 秒真实时间只推进约 1.4 秒
#    游戏时间）——截图前至少等待 2~3 秒真实时间，想看到小兵出兵/波次
#    效果需要等待相应倍数的真实时间，不要以为"等了 5 秒游戏时间该有
#    动静了"就去查 bug，先确认这个倍率关系。

# 平衡性测试（这次任务不涉及数值平衡，用不到，但以防要验证占领机制没被
# 意外改动）
node tools/balance_matrix.mjs --map dominion_crystal_scar_v1 --runs 5 --minutes 20
```

---

## 第五部分：当前状态（写这份文档时的仓库快照）

- 分支：`claude/game-model-pitfalls-tasks-1qwt22`
- 第三轮视觉改造（`terrainEdge`/巨岩/沿途色阶/据点地面色）已经完成并
  验证：`node tests/sim_dominionprops.mjs` 本地跑通（27 项断言全过），
  `node tests/run_all.mjs` 全量回归确认 **✅ 全部通过（0 失败）**——
  中途跑出过一次"2 套失败"，两个都排查清楚了，不是这次改动引入的真
  回归：
  1. `tests/sim_navoutline.mjs` 里一条老断言假设"只有嚎哭深渊冰封版
     声明了 `terrainEdge`"，随着 dominion 这张图合法接入这套机制，
     断言的排除名单需要跟着更新——**已修**（把 `dominion_crystal_scar_v1`
     加进排除名单，见 2.4 节表格最后一行）。
  2. `sim_full.mjs`（"全栈冒烟"）里"前期(240s)建筑无损=30"这条断言
     单独重跑（`node tests/run_all.mjs --only sim_full`）能稳定通过，
     跟这次改动的文件完全没有交集（这次只碰了统治战场相关文件，这条
     测的是经典召唤师峡谷地图）——判定为全量跑 41 套时的计时敏感型
     偶发抖动（240 秒模拟战斗对帧预算/GC 停顿比较敏感），**不是回归，
     没有修改任何代码来"修"它**。如果你以后也遇到这条断言在全量回归
     里偶尔红一下、单独跑又稳定绿，可以先按"已知偶发抖动"处理，不用
     每次都当成新 bug 去查——但如果它开始**稳定**失败（不止全量跑时
     偶发），那就是真的回归了，要重新排查。
- 这份文档和第三轮代码改动（`src/data/mapComposition.js`、
  `src/data/maps/dominion_crystal_scar.js`、
  `src/presentation/DominionPropsLayer.js`、`tests/sim_dominionprops.mjs`、
  `tests/sim_navoutline.mjs`）会在同一个提交里一起提交推送——用户的
  原始指示是"一块推送，一起做完"。

---

## 结语

如果你是接手这个任务的 GPT：**先把第二部分 2.2 节的用户原话完整读一遍**，
那是这次返工的直接起因，比我这份摘要更能让你理解"用户到底不满意什么"。
剩下的实现细节（哪个文件哪个函数做什么）第二部分已经写得足够具体，照着
文件清单去看源码注释（每个新函数上面都有详细的头注解释"为什么这么做、
对应用户或 GPT 的哪句反馈"），不需要我在这里再复述一遍代码逻辑。

祝交接顺利，别把撒石头当成解决方案。
